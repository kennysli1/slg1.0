# Travian 2.0 重构评审与AI执行指南（review1.0）

## 0. 文档目的

本文件用于指导 AI 基于当前代码库进行结构化重构，目标是：

1. 大多数内容新增通过表格配置完成（如新增兵种、建筑、PvE 内容）。
2. 大多数功能新增可通过阅读架构文档快速落地。
3. 整体可读性、易用性、可扩展性达到长期维护标准。

本文件分为两部分：

- 第一部分：当前项目架构文档（统一认知）
- 第二部分：建议修改文档（重构蓝图 + AI执行步骤）

---

## 第一部分：当前项目架构文档

## 1. 项目定位与技术栈

- 项目类型：服务端权威的实时 SLG 网页游戏（Travian 风格）。
- 工程形态：TypeScript monorepo（npm workspaces）。
- 包结构：
  - `packages/shared`：前后端共享协议与消息定义
  - `packages/server`：游戏服务端（Fastify + WS）
  - `packages/client`：前端（Vite + Vanilla TS）
- 数据层：
  - 配置数据：`config/*.csv`
  - 运行态持久化：`data/game.json`（JSON 文件存储）

## 2. 服务端架构分层

服务端总体遵循三层结构：

1. 接入层（Gateway）
   - 文件：`packages/server/src/gateway/gateway.ts`
   - 负责请求 action 到 command 的映射、会话鉴权、事件推送映射。

2. 领域层（Modules）
   - 文件目录：`packages/server/src/modules`
   - 主要模块：
     - `player.ts`
     - `economy.ts`
     - `building.ts`
     - `military.ts`
     - `world.ts`
     - `pve.ts`
     - `movement.ts`
     - `combat.ts`（无状态纯函数）

3. 基础设施层（Infra）
   - 文件目录：`packages/server/src/infra`
   - 核心能力：`CommandBus`、`EventBus`、`Scheduler`、`Store`、`CSV`、`Config`。

## 3. 关键设计原则（已形成）

当前代码已具备如下良好架构基础：

1. 状态归属明确：每块状态由单模块持有。
2. 跨模块通过 Command/Event 解耦。
3. 时间流程统一由 Scheduler 驱动。
4. Combat 无状态纯函数，便于 PvE/PvP 复用与测试。
5. 核心内容（资源/建筑/兵种/PvE）已 CSV 化。

## 4. 当前扩展链路（已可用）

- 新增兵种/建筑/PvE 内容：主要在 `config/*.csv` 新增行。
- 服务器启动时由 `packages/server/src/infra/config.ts` 解析并注入模块。
- 领域模块已大量使用 `GameConfig` 读取配置，而不是写死 defs。

## 5. 当前架构的主要短板

### 5.1 配置驱动与显示层不一致

- 客户端存在本地展示映射：`packages/client/src/info.ts`。
- 与服务端 CSV 配置形成双源，新增内容仍可能要求改前端硬编码表。

### 5.2 关键常量仍硬编码

典型硬编码点（示意）：

- `economy.ts`：`RESOURCE_TYPES`、初始资源、基础仓储容量、容量成长公式。
- `combat.ts`：城墙每级防御加成常量。
- `military.ts`：铁匠每级加成常量。
- `building.ts`：主基地建造加速常量、初始田地布局。
- `app.ts`：地图尺寸常量。

### 5.3 网关映射维护成本高

- `ACTION_ROUTES` 与 `EVENT_TO_PUSH` 需要人工维护。
- 新增功能时，AI 可能漏改网关映射，导致“模块已实现但前端无法调用”。

### 5.4 客户端单文件过重

- `packages/client/src/main.ts` 体量大，混合多页签与多流程逻辑。
- 不利于 AI 精准定位变更边界，提升误改风险。

### 5.5 配置缺乏强校验

- CSV 解析后缺少系统级 referential checks（跨表引用、非法值、循环依赖等）。
- 容易在运行阶段暴露问题，而不是启动阶段失败并明确提示。

---

## 第二部分：建议修改文档（AI重构指引）

## 6. 重构总目标（Target Architecture）

重构后目标状态：

1. 单一配置真源（Single Source of Truth）
   - 内容显示与数值逻辑统一来自服务端配置。
2. 常量配置化
   - 绝大部分平衡参数迁移到 CSV（或统一配置表）。
3. 自动化接线
   - Gateway 映射尽量由模块声明自动生成。
4. 前端模块化
   - 以 feature/tab 维度拆分，降低 AI 修改复杂度。
5. 配置安全网
   - 启动即校验，错误可读、定位到表/行/列。

## 7. 建议新增配置表（优先）

## 7.1 `config/game_constants.csv`

承载全局常量，建议字段：

- `key`
- `value`
- `type`（number/bool/string）
- `note`

建议迁移项：

- `wall_bonus_per_level`
- `smithy_bonus_per_level`
- `main_build_speedup_per_level`
- `main_build_speedup_cap`
- `storage_base`
- `storage_growth_per_level`
- `start_resource_amount`
- `map_size`
- `map_view_radius`

## 7.2 `config/village_templates.csv`

用于定义不同 tribe 的开局布局，建议字段：

- `tribe`
- `field_layout`（例如 `woodcutter*4|claypit*4|ironmine*4|cropland*6`）
- `start_buildings`（例如 `main:1|rallypoint:1`）
- `start_resources`（可选，覆盖默认）

## 7.3 （可选）`config/client_texts.csv`

用于可本地化文案（按钮文案、错误文案、标签名），减少前端硬编码字符串。

## 8. 关键代码改造建议

## 8.1 服务端：配置中心升级

文件：`packages/server/src/infra/config.ts`

改造目标：

1. 扩展 `GameConfig` 增加：
   - `constants`
   - `villageTemplates`
2. 增加 `validateGameConfig(config)`：
   - 跨表引用合法性（如 unit.building 必须存在）
   - 关键值范围校验（负值、零除、等级区间）
   - 依赖闭环检查（建筑 requires 不允许循环依赖）

## 8.2 服务端：去硬编码常量

按模块执行：

1. `economy.ts`
   - 从 `config.resources` 生成资源 key 列表（替代固定 `RESOURCE_TYPES`）。
   - 初始资源与容量改为配置读取。
2. `building.ts`
   - 开局田地布局改为 `villageTemplates`。
   - 建造速度修正改为 constants。
3. `military.ts`
   - 铁匠加成改为 constants。
4. `combat.ts`
   - 城墙加成改为 constants（通过调用方传入或配置注入）。
5. `app.ts`
   - 地图尺寸改为 constants。

## 8.3 服务端：统一对外配置接口

新增 action + command：`GetGameConfig`

输出内容最小集合：

- resources（name/icon/key）
- fields（type/name/icon）
- buildings（kind/name/icon）
- units（key/tribe/name/icon/cat）
- pveTemplates（type/name/icon）
- constants（仅前端需要的白名单，如 mapViewRadius）

目标：客户端渲染不再依赖 `info.ts` 本地常量。

## 8.4 Gateway：向声明式注册迁移

建议给每个模块加 manifest（可先手工实现，不做复杂代码生成）：

- `moduleName`
- `publicActions`
- `eventPushMap`

Gateway 在初始化阶段汇总 manifest 生成路由表，减少漏配。

## 8.5 客户端：按 feature 拆分

建议目录结构：

- `packages/client/src/app/`
  - `bootstrap.ts`
  - `state.ts`
  - `config.ts`（服务端配置缓存）
- `packages/client/src/features/`
  - `village/`
  - `army/`
  - `map/`
  - `reports/`
- `packages/client/src/shared/`
  - `ui/`
  - `utils/`
  - `types/`

落地规则：

1. `main.ts` 仅保留应用启动与路由壳。
2. 每个 tab 独立 render + event handler。
3. 统一 API 层响应封装，业务层不直接写底层 websocket 细节。

## 9. AI 重构执行流程（严格顺序）

## 阶段 A（基础收敛，低风险）

1. 增加 `game_constants.csv` 与解析逻辑。
2. 迁移 3 个核心常量（城墙、铁匠、主基地加速）并补测试。
3. 增加配置校验器（先覆盖跨表引用与范围）。

验收：

- 现有测试全部通过。
- 默认配置下游戏行为与改造前一致。

## 阶段 B（配置驱动闭环）

1. 增加 `GetGameConfig`。
2. 客户端移除 `info.ts` 强依赖，改为读取服务端配置。
3. 新增一个示例兵种（仅改 CSV）验证端到端显示。

验收：

- 新增兵种无需改客户端代码即可显示。
- 无路由映射遗漏。

## 阶段 C（结构升级）

1. 客户端 `main.ts` 拆分为 4 个 feature 模块。
2. Gateway 向 manifest 汇总机制迁移。
3. 文档更新（`PROJECT.md`、`07_扩展与代码规范.md`）。

验收：

- 文件职责清晰，单文件复杂度显著下降。
- 新增 action 的改动点减少（模块内自描述优先）。

## 10. AI 改造约束与自检清单

AI 执行时必须满足：

1. 不破坏四条架构铁律（状态唯一 owner、Command/Event、Scheduler、派生快照）。
2. 不在模块间新增直接依赖访问对方私有状态。
3. 对每次配置结构变更，必须同步更新：
   - `config/README.md`
   - 解析代码 `infra/config.ts`
   - 最少一个测试用例
4. 每个重构 PR 最多聚焦一个阶段，避免大爆炸。
5. 每次提交前必须执行并通过：
   - `npm run test:server`
   - 必要的客户端构建验证

## 11. 推荐交付物（给未来 AI 任务单）

每轮重构应产出以下内容：

1. 变更说明（目标、改动点、风险）。
2. 代码差异（模块级摘要）。
3. 配置变更清单（新增/变更 CSV 字段）。
4. 回归结果（测试命令与结果）。
5. 下一轮建议（最多 3 项）。

---

## 12. 结论

当前项目已经具备较好的模块化骨架与数据驱动基础。建议优先完成“常量配置化 + 服务端统一下发配置 + 客户端去硬编码映射 + 配置校验器”四件事。完成后，项目将从“半配置驱动”升级为“高比例配置驱动”，并显著提升 AI 的重构成功率与扩展效率。
