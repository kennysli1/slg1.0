# 变更日志

> **想知道"最近发生了什么"，只看这一个文件，不要去翻 `git log` 猜。**
> 记账规矩见 [`docs/00_变更契约.md`](./docs/00_变更契约.md) R5：改了 `packages/**/src/` 或 `config/*.csv` 的提交，
> 必须在 `## [未发布]` 下加条目，否则提交会被 `npm run guard` 拒绝。
>
> 分类固定：`### 新增` / `### 变更` / `### 修复` / `### 移除` / `### 破坏性`。
> 需要刷档的改动，条目开头加 **`[需刷档]`**（同时必须升 `SAVE_SCHEMA_VERSION`）。

## [未发布]

### 新增

- 联盟大厅等级上限提升至 10；后勤主管、战争专家、首席科技官、形象大使分别在联盟 1/2/3/4 级解锁。联盟声望取当前形象大使声望（无形象大使为 0），按配置中心倍率缩放联盟职位、建筑和科技加成；新增形象大使专属王国资源/增援服务目录，服务通过联盟大厅商队或行军抵达后结算。
- 已加入联盟的玩家新增“所有联盟”目录（支持按联盟名/盟主搜索，并显示等级、声望、成员数）；联盟大厅失联时撤回前往大厅的服务运输，取消未出发战事，清空未完成项目、仓库资源和未投入科技点但保留历史贡献、已建建筑与已研发科技，已在路上的联盟战事继续，重建后按大厅等级重新锁定职位与项目。
- 联盟战事改用倒计时创建目标；参战兵力改为按来源村逐兵种选择，盟主/战争专家可在报名截止后的 90 秒固定窗口取消行动或撤回已派出部队，窗口不会因晚派出的远程军队顺延。
- 联盟战事面板新增参战成员与派兵明细，按玩家显示来源村、参战状态、各兵种数量及派出总数。
- 联盟战事成员可按现有军队规模选择随队宝物，服务端在预览、报名和实际派出时校验宝物持有数量与携带上限；盟主/战争专家只能在报名截止后的 90 秒窗口取消行动并撤回已派出军队。
- 联盟战事参与者快照记录所选宝物并在派出时交给通用行军流程；普通攻防/防御宝物只作用于携带该宝物的军队，绞马索等敌方单位削弱效果按战场阵营聚合，联盟多军队不会重复发放击杀奖励。
- 联盟战事新增独立参军倒计时（战争倒计时与参军倒计时均支持时/分/秒输入）；报名兵力在来源村形成服务端预备队锁，成员可在截止前取消报名，盟主/战争专家可在报名截止后 90 秒固定窗口内取消行动并自动撤回已派出军队；军队页新增联盟战事预备队明细。
- 联盟建筑与联盟科技支持在尚未开工时随时更改规划；资源/科技点满足后自动扣除并开始计时，默认 10 秒，耗时可在配置中心的联盟参数中修改。
- 联盟项目完成改由 Scheduler 延迟结算，成员名单新增四个职位格，显示任职玩家、解锁等级及当前职位效果。

- 联盟资源贡献改为贸易中心商队运输：来源村必须建有贸易中心并拥有足够空闲贸易路线，发车时扣除资源并锁定路线，商队抵达联盟大厅后才入库并计入成员贡献；运输中状态可在联盟建筑页查看，资源不足、无贸易中心或路线不足会返回明确提示。

- 资源栏悬浮提示新增产出组成明细：资源田基础产值、宝物、科技、联盟、任务及定时加成会分别列出每小时贡献和永久/剩余持续时间；粮食同时显示劳动人口与军队耗粮。人口和金币也分别展示主基地基础增长/劳动人口税收，以及宝物、科技、声望、任务等来源。

- 配置中心新增“联盟参数”专用板块，集中编辑联盟创建成本与四类职位加成；联盟等级、联盟建筑、联盟科技目录在该板块前置展示，修改后仍分别写回对应 CSV 并沿用热重载/同步流程。

- 新增联盟系统：联盟大厅创建与升级、联盟列表搜索/申请、成员与职位管理、联盟仓库建筑、联盟科技贡献、集结战事计划及成员统计；新增 `alliance_levels.csv`、`alliance_buildings.csv`、`alliance_tech.csv` 与联盟创建/职位加成配置，联盟大厅摧毁时进入失联状态并在重建后恢复。

- 地图选中其他玩家村庄时显示公开的玩家名、声望、人口与主基地阶段名称；地图图例移至左上角，并在移动端保持可滚动显示以避免遮挡操作和详情栏。
- 新增主线任务 M16「树立威望」：前置 M12，属于“开眼看世界”任务线，声望达到 10 后奖励 10 名无期限佣兵弓手；新增“另一边的故事”任务线作为 M13 入口，并将 M13–M15 迁入该线。
- 恢复骰子支线原任务「这下来真的了」（s7）：使用「投骰子竞标赛入场券」后解锁，支付 100 金币，以简单 NPC 三局两胜达到 2000 分并奖励 200 金币；原新增任务顺延为「有高手」（s8）和「巅峰对决」（s9），分别使用普通/困难 NPC、4000/6000 分目标，奖励 300/400 金币，s9 另奖励「骰子奖杯」。三项三局两胜任务放弃对局累计两局即失败，失败无奖励并通过失败对话确认。
- 新增宝物「骰子奖杯」（`dice_trophy`），价值 600 金币，装备在主宝物栏时被动增加 10 点声望；任务接取费用新增 `quests.csv.acceptCost` 配置并在任务卡展示。
- 新增三族远程专精兵种与“复合兵装”科技线：解锁罗马不屈军团、高卢战獠王骑、条顿黑钢卫队；补充齐射、拒马、战鼓、裂甲和游走特性，保持旧兵种代码兼容。
- 配置中心同步状态新增 PR 合并状态、CI 检查和冲突文件展示；`/config` 可逐文件查看配置中心/main/PR 内容、编辑最终 CSV 并提交双父冲突解决提交，配置中心值仍是权威且提交前执行整图校验。
- 新增独立阶段化战斗模拟器：服务器端无状态 `battle-simulator` 模块与 `/battle-simulator` 页面只读取 CSV 兵种、特性、PvE 守军、科技、宝物和全局常量，不读写主游戏存档；支持自由编队、伏击/掠夺/遭遇/攻城模式、科技/宝物/城墙加成、可复现随机种子，并输出各阶段攻防/生命伤亡池、损失分配和特性目标报告。
- 兵种、雇佣兵和 PvE 守军新增模拟器生命值字段；`units.csv.simTraits`、`mercenaries.csv.simTraits`、`pve_defenders.csv.traits` 与 `unit_traits.csv` 可在配置中心编辑。禁卫兵/方阵兵/长矛兵配置反骑兵，军团兵配置协同远程防御，斧兵配置破阵，非侦察骑兵配置冲锋加成，其余雇佣兵/NPC 特性暂为空。
- 新增自上而下的科技树阶段布局；补充侦察学说、攻城工程、兵工组织、探索特许与拓荒章程等解锁型科技，分别开放兵种、建筑与建村能力。
- 新增独立筛色子实验场 `packages/dice-lab`：普通六面骰规则、热骰/爆骰、三档 NPC、服务器内存会话、静态 Preact UI 和生成的骰子/计分板美术；通过 `kow-dice-lab` PM2 服务和 `/dice-lab/` 同域入口部署，与主游戏存档、Wire 协议和领域状态隔离。
- 新增支线任务「猎马人」（二级主基地后进入酒馆支线池），跨战斗累计击杀 50 人口骑兵后奖励宝物「绞马索」。
- 新增 `cavalry_unit_codes` 配置和 `enemyCavalryDef` 宝物效果；绞马索使携带军队面对的敌方骑兵防御降低 30%，并接入 PvE/PvP/野战战斗快照。
- 配置中心 `/config/balance` 新增“猎马人 / 绞马索参数”专用区，可直接修改猎马人目标人口与绞马索骑兵防御削弱值，并分别写回任务目标与宝物 CSV。

### 变更
- 配置中心冲突检测改为基于 PR 合并基点的三方逐行/逐字段比较，只把真实冲突行列列入解决列表；冲突状态和页面会显示具体文件、稳定主键与字段，无法定位时明确提示原因。

- 联盟战事历史记录按目标倒计时判断活跃状态；已派出但已过目标时限的记录会自动折叠，报名行军预览移除冗余的“若现在出发，预计……抵达”时钟文案。
- 商队最低行进时长改为配置中心 `trade_caravan_min_duration_sec`，默认 3 秒；移除原先固定 3000 秒（50 分钟）下限，商队速度 100 格/小时按实际距离生效，已在途商队不受修改影响。

- 商队速度改为配置中心 `game_constants.csv` 的 `trade_caravan_speed` 参数，默认 100 格/小时；新增“贸易参数”专用编辑板块，修改只影响新派出的商队，已在途商队保持原定到达时间。

- 联盟失联时从公开联盟目录隐藏，成员页改为只读的所有联盟列表；保留的加入申请待大厅恢复后由盟主统一审核，已加入联盟的玩家不再看到申请按钮。联盟战事按活跃目标优先、创建时间倒序排列，历史目标默认折叠；地图选定目标后自动返回联盟战事页，并按目标类型限制攻城、掠夺和增援（普通 PvE 无攻城、盟友仅可增援、个人任务营地不可选）。参战成员已派出后不再显示取消参军按钮。
- 联盟战事历史记录在战事结束后自动收起；成员选择出兵时通过服务端权威预览显示当前兵力的预计行军时长、立即出发的预计抵达时间及最长允许行军时长，并明确标记是否符合报名限制；盟主/战争专家在报名期间即可取消行动，报名截止后仍保留 90 秒取消并撤回窗口。
- 联盟预备队现在统一从普通驻军、解散、防掠夺、地图出兵、侦察和拓荒可用兵力中扣除；同一玩家可在同一联盟战事中多次报名，服务端逐次锁定并在联盟页面按玩家/来源村合并显示兵力、预计行军时间和随队宝物。联盟页面收到 `AllianceUpdated` 推送后专用实时刷新，无需重新进入页面。

- 军队页将“防御掠夺”调整到训练面板下方、解散面板上方，并改为与解散军队一致的可折叠区块，保持箭头、键盘和展开状态交互统一。

- 恢复配置中心作为全部 CSV 的唯一权威来源；此前对 `dialogues.csv`、`units.csv` 的拆分仅用于识别当时哪一端数据较新，不改变长期同步规则。保留当前已同步数据，并恢复配置中心编辑后经共享配置、GitHub PR 和发布端合并到各端的流程。
- 配置同步回归测试改为校验配置中心显式按钮和原样透传，不再用出厂默认断言覆盖合法调参。
- 优化地图玩家村庄详情页的视觉层级：公开资料改为紧凑的两列信息格，缩小字号与间距，并收紧目标评估区，提升桌面和移动端的可读性。
- 重构战斗影响力模型：基础战斗价值继续按近攻 1.00、远攻 1.10、近防 0.85、远防 0.75、生命 0.65 计价，并按 `popCost` 归一后以 1.15 指数映射有效战斗人口；线上兰开斯特推进改用有效人口，阶段模拟改用攻防侧质量池，特性不计入基础战斗分值并单独结算。
- 战斗伤亡采用平滑除法公式下的有效人口回写，兼容旧快照时自动补齐 `popCost`；补充同人口同比兵种、硬克制、混搭和高科技兵种的模拟回归，避免低于防御时不破防及高人口兵种被少算战力。
- 重构核心兵种基准：盾墙军团锚定约 200 战斗价值/人口，拒马长矛强化近战承伤但保留低远防，罗马弩阵强化远程输出，冲角骑兵按 2 人口参战；新增 `units.csv.techTier` 作为 1/2/3 档科技标签，不直接乘战斗力。
- 对话编辑器改为先按 taskCode、再按 code（下划线分段优先、数字感知）、段落号和 id 排序，使 t1/t2 使用对话不再置顶。
- 骰子王三局两胜牌桌：小局结算时新增“下一局”按钮，可直接开始下一局；达到两胜、任务成功或失败后只保留“退出对局”。
- 阶段战斗伤害采用带优势放大的平滑除法公式，并用小数伤亡累计池跨轮结算：模拟器不再因攻击低于防御而完全不破防，也不再把每轮小额伤害强制向上取整；线上逐 tick 将战斗强度调至 0.15，并设置 180 tick/36 秒安全上限。
- 按 1 人口步兵、1 人口远程、2 人口骑兵基准重新校准常规兵种人口效率：基础无特性矩阵形成远程→长枪→骑兵→远程的明确克制链，长枪/斧兵混搭能压制单一骑兵；阶段战术窗口调整为 8 轮，窗口结束按 1.15 倍有效战力判定优势；特性仍不计入基础属性分值并提供独立对照模拟。
- 阶段化数值再校准：以“一次骑兵冲锋→一次远程打击→8轮近战”为唯一平衡基准，采用 `k×A×A/(A+D)×(1+g×max(0,(A-D)/(A+D)))` 除法伤害公式，近战轮次系数为 0.30、优势放大系数为 1.50；重做核心步兵、远程、骑兵与 T2/T3 复合兵的面板和阶段特性；阶段模拟器只读取 `simTraits`，不再把线上 `traits` 重复计入。
- 兵种训练卡恢复显示 `units.csv` 基础攻防与速度；`GetArmy.trainable[]` 新增兼容性的 `baseStats` 基础快照，原有最终属性字段及战斗结算仍保留科技、宝物和声望加成。
- 修复主游戏 NPC 回放在连续掷骰时错误重置本轮累计分，服务端事件携带阶段累计并改为仅在收下或爆骰时结束阶段；爆骰回放在当前时长基础上缩短 0.25 秒。骰子牌桌动作绑定开桌玩家/任务村，切换当前村不会再触发 `dice_session_not_found` 或把放弃局写到错误村。
- 筛色子主游戏在收下本轮分数的回放期间保留玩家已选骰子高亮；玩家与 NPC 爆骰揭示在原有基础上再延长 0.5 秒，并补齐爆骰前最后一次保留组合的阶段明细，红色提示出现前冻结阶段/总分面板，避免提前剧透。S7 放弃局数持久化，第二次放弃直接失败；退出牌桌改为幂等清理，避免重复关闭出现 `dice_session_not_found`。入场券使用对话继续绑定 `t2/use`，并补充端到端回归测试。
- 修复配置同步覆盖的对话动作：恢复 `m11_deliver` 第二段的“收下”回复与 `s3_accept` 的“离开”选项，确保多段交付和接取对话可正常继续。
- 修复配置同步遗漏：恢复 S3 接取对话的“离开”选项、M11 多段交付对话的“收下”回复，并让天王老子村守军使用条顿棍棒兵的权威属性；同步更新旧测试断言以匹配当前 CSV。
- 运维：生产发布验收成功后默认保留当前及最近 4 个 release，自动清理更旧的代码目录，避免历史 release 持续占满磁盘；`shared/data`、`shared/logs` 与 `shared/config` 不受影响。
- 统一科技效果文案，移除“再”等递进补充字样；攻击、防御与行军加成按近战/远程/专项方向独立展示。
- 修正兵工厂的主基地前置等级，使其与当前四级主基地上限一致，可由兵工组织科技正常解锁。
- 收敛地图红色提示规则：普通 PvE 不再常驻红色外框，视野内返程/驻扎/运输/探索等外军改用非红色标记；红色仅保留给仍在行军或交战中的进攻/掠夺军和有效来袭预警，选中目标统一用金色强调。
- 精简科技树副文本，改为按研发阶段排列节点并显示实际效果对应的专精标签；军事科技明确拆分近战攻击、远程攻击、近战防御、远程防御与行军速度方向。
- 战斗逐 tick 伤亡改用兰开斯特平方律解析推进：双方同时按 `dA/dt=-βB`、`dB/dt=-αA` 计算，人数优势会在战斗过程中持续放大，突出大规模部队的战斗影响。
- 优化村庄建筑与城务工作区的桌面端布局：村庄指挥台与人口面板上下堆叠展示，手机端继续自动保持单列。
- 生产构建、部署启动与冒烟检查纳入独立实验场；新增 `test:dice-lab` 与 `scripts/dice-lab-smoke.mjs`，服务不可用时主游戏代理返回 503 而不影响主游戏路由。
- 筛色子补齐普通计分表：加入 1–5 顺 500 分、2–6 顺 750 分，并移除未列入规则表的三对计分；回合面板显示前面阶段已保留骰的组合与分值。
- 筛色子保留骰按 KCD2 回合规则只累加到当前未收下分数、不与后续骰子重组；爆骰事件携带失败点数，客户端停留约 2.5 秒展示结果后再进入 NPC 回合。
- 筛色子允许 1 点和 5 点作为额外单骰与顺子或同点数组合叠加；NPC 回合改为逐动作展示服务器牌面与选骰，达标计分板显示标记，结算牌面、胜负和牌型会保留到点击“再来一局”。
- 筛色子爆骰改为在同一牌桌停留约 3.2 秒展示失败牌面与提示，再开始 NPC 回合；NPC 骰子改用与玩家一致的桌面骰子样式，动作间隔延长至约 1.35 秒。
- 筛色子 NPC 回合改用蓝色牌桌与蓝色分数，按播放进度显示 NPC 总分及阶段保留分数；NPC 爆骰才切换红色提示，未选骰子隐藏“可用”标签，仅保留骰子显示“保留”。
- 主游戏骰子王牌桌补齐当前选骰计分、前阶段保留组合与分值、实验室同款积分规则说明，并增加可展开的历史操作记录；NPC 回合分数与局数改为在动作播放完成后更新，播放期间锁定操作，避免先显示结算分数再展示 NPC 动作。
- 修复 NPC 达标时通用胜者高亮覆盖蓝色分数的问题；NPC 获胜仍保持蓝色高亮，目标标记单独保留醒目效果。
- 玩家爆骰改为先展示新摇出的骰子约 0.9 秒，再切换红色爆骰提示；顶部计分板始终保留，避免爆骰流程造成页面上下移动。
- 玩家爆骰牌面展示期间不再卸载本轮计分明细；揭示阶段仅显示普通桌面骰子，短暂停留后才显示红底爆骰提示并进入后续流程。
- 玩家爆骰揭示阶段不再套用蓝色牌桌或读出样式，保持普通桌面底色；短暂停留后才切换红底爆骰样式。
- NPC 牌面区域补足到玩家骰子区域的高度，并统一上下间距，减少回合切换时的画面跳动。
- 新增主游戏支线「骰子王」：s6 在酒馆挑战简单 NPC，失败可重新尝试并在胜利后获得「投骰子竞标赛入场券」；使用入场券解锁 s7 三局两胜挑战（入场费 100 金币，胜利奖励 200 金币）。主游戏采用独立骰子任务 session 与牌桌弹窗，不依赖骰子实验场页面；退出进行中的单局计 NPC 胜一局，任务结果在任务栏继续处理。

### 修复

- 修复联盟成员在前往同一目标途中同格时被普通遭遇战或伏击战误判为敌军的问题；遭遇与伏击现在都会查询联盟权威关系，盟友军队不再互相交战。

- 修复联盟战事目标校验将所有任务营地误判为个人目标的问题；仅个人可见的任务 PvE 营地会被拒绝，公开的主线任务村仍可作为联盟战事目标。
- 修复联盟战事取消与行军预览抖动：盟主/战争专家在报名期间即可取消行动，报名截止后仍保留 90 秒取消并撤回窗口；行军预览不再随每秒倒计时重复请求，加入行动按钮状态保持稳定。
- 修复联盟大厅受损空壳因单村建造上限从建筑清单消失的问题；大厅修复/重建入口会继续展示给符合条件的盟主。
- 更新服务端 Fastify 及其 URI 解析依赖到已修复版本，消除生产依赖的高危 `fast-uri` 审计告警。

- 修复放弃骰子对局后仍回放 NPC 回合、弹出本局结算的问题；确认放弃现在直接记录 NPC 胜局、刷新任务状态并退出牌桌。前面阶段保留面板预留首条记录高度，首次出现组合时不再推动页面布局。
- GM 任务管理为已完成主线、已完成支线和已放弃支线提供两种重新触发方式：返回未触发/可接取状态，或直接接受并立即创建任务实例；已放弃支线的“返回未触发”会清掉触发标记，直接接受则跳过触发条件；两种操作均复用服务端状态清理和任务营地初始化逻辑。
- 修复骰子王牌桌打开“放弃对局”确认框时因弹层回调变化而重复开桌、继而报 `dice_session_not_found` 且不记录 NPC 局分的问题；失效牌桌上的已确认放弃会自动重开并补记败局。牌面、阶段分、总分与多局比分改为从同一逐帧回放状态推进，事件按上一帧实际展示完成后再播放下一帧，缩短普通动作和爆骰等待并减少网络抖动造成的跳帧、卡顿与计分错位。
- 修复配置中心对已合并 GitHub PR 的状态解析：GitHub 返回 `closed` 且带有 `merged_at` 时现在显示为“已合并”，不再误报为“PR 检查中”。
- 修复配置中心首页冲突处理脚本的引号转义错误；页面现在能正常加载同步状态，并可打开冲突详情与人工解决编辑器。
- 修复阶段化战斗模拟器的科技与宝物选择控件只能依赖 Ctrl/Cmd 多选、无法取消已选项的问题；改为可逐项勾选/取消的多选列表，并提供清空操作，所选数组会完整传给模拟请求。
- 修复王国首页桌面端展开军务后内容掉到双列工作区底部的问题；桌面端建筑与军务各自保持独立列，手机端继续按单列顺序显示对应展开内容。
- 修复战斗损坏建筑修复完成后人口硬上限未恢复的问题；人口模块现在同时响应战斗损坏与修复完成事件，并统一重算建筑派生上限。
- 修复声望高于封地报复阈值时错误派出王国复仇军的问题；只有达到配置的掠夺阈值（及更低）才会触发，攻城阈值仍单独生效。
- 修复王国复仇军战斗结束后返程记录丢失 NPC 标识、因而不出现在玩家视野的问题；返程保留 `npcService`、`taskCode` 和来源封地信息。
- 修复战斗准入失败后行军可能永久停在 `paused` 的问题；Movement 现在检查 `combat.Engage` 结果，失败会恢复为原路返程。
- 战斗结算改为可恢复的 `resolving` 状态；域状态、攻击方战报和守方战报分阶段持久化，重启后会继续未完成步骤，不再把未完成结算误当作已结束战斗跳过。
- 修复王国工作区切换会意外收起另一个已展开面板的问题；建筑与军务状态现在独立切换，移动端展开内容紧跟对应页签显示。
- 优化任务目标展示：修复资源田任务改为带编号的逐项清单，完成后显示勾选状态；任务奖励固定在左侧，目标内容在右侧形成清晰的双栏布局。
- 修复旧存档中的王都/封地仍被识别为普通 PvE、因而没有“攻城”选项的问题；启动恢复和首次读取目标时都会按 `pve_targets.csv` 的王国城市配置完成迁移，并同步地图标记。
- 修复任务面板内容按最短文本收缩、桌面端右侧留白的问题；任务说明、目标、奖励与操作现在按可用宽度铺开，奖励资源和宝物图标统一正确加载 WebP 资源。
- 修复王国首页工作区展开仍被限制在半宽卡片的问题：摘要卡保持双列，展开后的建筑与城务或军务操作区改为占据其下方整行面板。
- 修复任务接取与交付对话把“关闭”误当成“推进”的问题：Accept、自动等待对话和失败对话的 X、遮罩与 Esc 现在只终止当前 session；自动等待对话关闭后会立即消费，不再刷新重弹或展示后续段落。
- 修复点击任务栏“领取奖励”即提前完成任务的问题；客户端改为先调用只读 `task.StartDeliver` 展示奖励预览，只有首次点击 `take:收下` 才在服务端互斥锁内正式结算，失败保留待领取状态，后续段落不会重复发奖。
- 修复生产发布反向覆盖配置中心的问题：共享 CSV 的非空值、明确空值和配置中心新增行现在都保持权威，编辑器删除行会写入持久化删除记录；Git 配置只补充从未出现的新列/新行，不再让 `m11_deliver` 等已清空回复或已删除段落在部署后复活。
- 修复战报时间线依赖接收顺序的问题；历史与实时战报现在统一使用服务端事件时间降序排列，实时推送保留服务端时间戳。
- 修复目标消失掉头时并发刷新用旧行军快照覆盖返程状态的问题；掉头推送现在立即携带完整返程快照，并由客户端刷新版本闸门拒绝过期路线，避免标记回跳一格。
- 修复王国页资源账本按内容宽度收缩、右侧留下大块空白的问题；资源格现在会横向填满当前指挥条的可用区域。
- 修复驻扎军可通过坐标伪装将目标玩家村庄当作空地续行的问题；服务端现在统一按 World 权威地块识别玩家控制格，驻扎军不再提供或接受对玩家村庄的续行模式，王国地标、普通 PvE 营地与任务营地仍保留原有续行规则。
- 修复目标消失时返程计时与逐格调度不一致的问题；返程现在按出发后实际经过时间计算，并沿已走路径的反向前缀从掉头格开始绘制，避免中途掉头提前归队或视觉瞬移。
- 修复目标消失/撤回发生在两格之间时地图标记回跳到上一个格心的问题；现在下发掉头瞬间的段内进度，客户端从实际位置无缝切入返程，不改变服务端离散判定与返程总时长。
- 修复交付奖励弹窗无视对话配置、始终硬编码显示“收下”按钮的问题；现在只有当前 deliver 段配置了 replies 才显示回复按钮，没有回复时仅保留右上角关闭。
- 修复任务营地或其它 PvE/村庄目标从地图消失后在途军队未立即返程的问题；目标移除事件现在由地图 owner 同步广播，任务营地清除也会立即使目标失效。
- 修复 M11 交付奖励弹窗只显示第一段对话的问题；补齐 `m11_deliver` 的 segment2，并支持奖励弹窗按段落继续阅读。
- M12 使者对话的 `{villageName}` / `{fiefName}` 变量统一由服务端按当前玩家村庄与所属封地渲染；配置中心、运行时热加载、持久化同步和客户端下发保持同一变量契约。
- 修复王国 NPC 行军被玩家军队误判为普通野外遭遇或伏击目标的问题；王国任务/封地复仇/增援行军现在不会触发或参与这两类战斗。
- 修复王国 NPC 行军因内部来源 ID 无法解析玩家归属而不出现在地图视野的问题；视野内现在会以“王国军队”脱敏显示，包含复仇军返程。
- 修复王国地标迁移把持久化的显式空地误判为占用，导致西南封地缺失第三格且重启无法自愈；王都与四封地统一补齐权威占地，并增加全地标重启回归测试。
- 王都与封地的合并外框改为真正只有三条边的单一大三角，底层重铺同地貌底色以遮住内部格线；图标与名称按大三角中心定位，只有鼠标悬停时才高光整块区域。
- 修复王国地标客户端分组根因：缺少 `landmarkCenter` 的旧地块不再被逐格误判为中心；所有王都/封地统一按同一视觉占地合并为单组，不再因西南封地方向产生重复轮廓。
- 取消王都与封地的蓝/黄占地填充与内部单格选中环；地标继续作为一个合并目标保留居中图标、名称与完整点击区域。
- 王都与封地恢复普通 PvE 风格的红色合并外轮廓；常态不显示填充高光，仅悬停时高光整个三角形，内部不显示格线。
- 修复多段对话持久化配置合并误把共享 `id` 判为重复；对话行现在按 `code + segment` 精确同步。

### 新增

- 新增 Combat 内部生命周期分层：`combat/engine.ts` 纯逐 tick 引擎、`combat/loot.ts` 战利品规则、`combat/resolution.ts` 伤亡规划、`combat/types.ts` 状态类型；复合 `targetKind:targetId` 战斗键隔离不同目标类型。
- 新增战斗重构回归测试：纯引擎输入不可变、按来源拆分伤亡与运力、`resolving` 战场重启续结算。
- 新增 `task.StartDeliver` 公网动作，统一返回动态奖励预览、任务执行时记录的奖励村庄和完整 deliver 段落；空白或历史异常对话自动降级为“任务奖励已准备好 / 收下”，Wire 协议升至 v13，不新增存档字段、无需刷档。
- 村庄资源栏新增人口格，持续显示总人口与硬上限，悬停可查看劳动人口、军队人口和训练中人口；资源停产及人口满员时仍显示解除限制后的当前理论产量或潜在人口增长速度。
- 地图上的玩家村庄现在按外交关系使用不同边框：中立为钢蓝、盟军为翡绿、敌对为绯红；地图图例与悬停说明同步显示当前关系。
- 新增玩家村庄1–4本四套逐级升级的地图图标；主基地等级改变时由 World 统一更新对应图标，旧存档启动后自动同步，无需刷档。
- 王国 PvE 声望改为跨战斗累计消灭人口：每 25 人扣 1 点声望，每累计 5 点扣分检查一次报复；声望 ≤ -10 时对应领主封地派雇佣军掠夺主城，声望 ≤ -20 时改为攻城。
- 王都与四个领主封地统一接入王国 PvE 城邦机制，按独立高等级标准随机生成种族兵种、城内外建筑、资源、掠夺防守兵力，并使用同一套受击重置的兵力/资源恢复规则。
- 封地报复部队使用随机雇佣兵，兵力为封地守军总人口的 40%–70%；战后幸存者携带战利品返回封地并解散，不加入守军。

- 地图随机生成可配置数量的王国城邦 PvE 目标：随机资源、守军、防掠夺兵力、城内外建筑（四类资源田至少各一座 3 级），支持侦察、掠夺与攻城。
- 城邦攻击/侦察被发现的声望惩罚、兵力与资源线性恢复、恢复中受击重置，以及城邦专属建筑掠夺结算均接入现有 Movement/Combat/PvE/Scheduler 边界。
- 城邦与森林/丘陵/平原地形参数统一纳入配置中心。
- 王国城邦改为一级/二级/三级随机生成：分别为 3/4/5 种同族兵种，数量范围为 0–20、5–35、10–50，资源量随等级提升；既有城邦按生成规则版本自动重生成。
- 配置中心新增“地图格子特性 / 地形参数”和“王国城邦参数（三级/三种族）”专用板块，侦察模式可明确选择资源守军或城内外建筑。
- 城邦兵种改为完全随机抽取，不再强制包含侦察兵；生成规则版本升至 3，既有城邦会自动重新生成。
- 王都地图占用 6 格三角形，四块领主封地各占用 3 格三角形；中心格保留唯一 PvE 交互目标，旧世界启动时自动补齐地标占地。
- 王都与领主封地改为上窄下宽的倒三角；地图将占地合并为无内部格线的大三角形，整块悬停命中并仅强调外轮廓，地标图标按整体轮廓等比例放大、居中并裁剪在边界内。

- 新增主线任务 M14「一份礼物」与 M15「小试屠刀」并归入「开眼看世界」任务线；M14 上交 500 粮食后按正声望兑换无期限佣兵剑士，M15 以声望 ≤ -5 为目标并奖励科研点与粮食产量加成。
- 新增主线任务 M12「使者来访」：前置 M11 与议会厅，清理两处强盗任务营地；最后一处通过报告发放任务宝物「我的努力」，手动交付后获得 5 点声望与 50 金币。
- 新增主线任务 M13「寻找神秘人」与任务村「秘密营地」：使用「我的努力」后，在主城第二近的连片丘陵随机生成秘密营地；调查行军抵达后不战斗并驻扎，掠夺则进入任务失败流程。完成调查后手动领取「黑色徽章」。
- 新增宝物「我的努力」与「黑色徽章」；宝物使用对话使用 `<treasureCode>_use` / `trigger=use` 绑定，黑色徽章提供军队攻防与 PvE 掉率被动加成。
- 新增调查行军模式与 `investigate_task_village` 任务目标；调查军抵达指定任务营地后停驻并通知任务模块，不触发 PvE 战斗。

### 变更

- 精简质量闸门：提交按变更范围验证，push 不重复本地全量测试，Linux release 布局与全量产物冒烟统一由 CI 承担；存档版本升级改为先做迁移评估，不自动触发刷档。
- 酒馆到达刷新周期时现在会重新抽取并替换所有尚未接取的任务；启动恢复和酒馆建成/升级仍只补齐空槽，已接取任务不受影响。

- 军队行军速度现在按本次实际携带部队的 `popCost` 总人口缩放：在既有兵种、科技、全局与地形计时后逐段应用规模减速，返程按幸存兵力重新计算，商队固定速度保持不变。

- 配置中心现有 `deliver` 对话统一补充 `take:收下` 回复，新增支线任务模板也默认带该回复；对话编辑器提示同步说明交付按钮由 replies 配置控制。

- 配置中心“声望参数”统一汇总所有声望输入与输出：王国任务权重/奖励/门槛、王国 PvE 累计扣分与封地报复、任务声望目标/效果、正声望兑换佣兵、宝物声望值和议会厅服务声望价格；保存时仍按所属 CSV 写回并统一校验。

- 移除侦察、掠夺和攻城的固定声望扣除，改由王国 PvE 战斗结算统一按消灭人口累计扣分；相关人口阈值、报复阈值和封地/王都生成参数已加入配置中心。

- 任务营地改为在村庄周围的配置搜索范围内从全部空地稳定随机选点，不再按距离由近到远固定命中紧邻村庄的格子；不同任务营地使用独立选择种子，重启后结果仍可复现。
- 森林地貌现在会按方向削减军队视野，丘陵地貌为军队提供额外视野但使离开丘陵格的行军速度降低三分之一；行军仍取最短路径并逐段计入丘陵耗时。
- 拓荒目标增加平原校验，森林和丘陵格不可建立新城。
- M12/M13 的任务营地由任务模块按任务状态创建和清理，使用稳定任务 ID、任务所属玩家和 World 权威坐标，避免重启或重触发时复用错误的 PvE 实体。
- 任务营地不使用普通 PvE 掉率：M12 仅最后一处营地固定产生「我的努力」，M13 秘密营地固定使用 CSV 守军与任务库存；任务专属宝物不会从其它临时营地随机掉落。

- 修复配置同步提交的生产验证路径：配置中心的可调参数使用配置专用全图校验，普通代码提交继续执行完整回归测试，避免合法调参被出厂默认值断言阻断发布。
- 提交与推送钩子按运行时改动范围选择验证路径，避免配置同步后的合法调参让仅运维/文档改动无法推送。
- 旧版 `balance_overrides.json` 迁移成功后归档遗留源文件，避免每次发布重复生成相同配置 revision/PR。

### 修复

- 修复王都/封地切换倒三角后旧版本占地未清理，导致新旧三角形叠加成多块轮廓；启动迁移现在只保留当前占地并清除同地标的遗留格，同时移除地标中心的单格 PvE 内框。
- 战斗结算现在等待 BattleEnded 的行军处理完成，避免客户端或测试在幸存者返程尚未落库时读到空兵力。
- 修复配置中心对话表格自然排序正则在 HTML 模板中反斜杠被转义的问题；浏览器端现在真正按 `code` 的下划线分段、数字感知规则排序，不再显示 CSV 原始乱序。
- 对话编辑器及其保存接口统一按下划线分段、数字感知的 `code → taskCode → segment → id` 排序；下划线分隔优先于后续数字，`m2`–`m9` 会排在 `m10`–`m19` 前，同一对话对象的段落顺序保持稳定。
- 驻扎军续行改为复用服务端目标/外交模式清单；PvE 目标可按条件显示侦察、调查与掠夺，玩家村庄按己方、盟军、中立或敌对关系显示转移、增援、侦察、掠夺和攻城等全部可用行为；伏击军在空地续行保持伏击语义。服务端会再次校验模式并保持原编队与行军点执行。
- 进一步收紧续行规则：混合普通兵种的驻扎编队不再出现侦察选项，伏击只能从城镇直接派出，已抵达伏击点只能召回；服务端与客户端共同校验，防止伪造模式。
- 黑色徽章宝物描述补齐军队攻防与 PvE 宝物掉落 +5%（无需携带）；资源栏声望提示补充当前村庄的实际 PvE 掉宝率（基础概率、声望倍率和宝物加成）。
- M13 秘密营地坐标按视野脱敏：生成时在玩家视野内才立即标记，视野外不向任务快照或地图下发坐标，探索发现后才显示并保留任务标记，同时附带任务名称与目标信息。

- 更新配置敏感回归测试，改为读取当前配置中心参数并验证不变量，避免管理员调参后被旧出厂数值断言阻断 CI。

- 配置中心生成的 CSV 同步 PR 使用专用校验路径：保留构建、静态检查、全图配置校验和运维布局测试，不再用固定出厂数值的单元断言阻断合法调参；普通代码 PR 仍执行原全量测试。
- 战报筛选移除已不再产出报告的建造、训练、行军、宝物、警报、人口和科研类别；战斗报告展开按钮改用居中的几何箭头，避免字体字形偏移。
- 战斗报告展开后不再挤压摘要，回放改在卡片底部全宽呈现；主导航新增独立“任务”页签，并将待接取任务提示移至该页签。
 - 主基地建造格统计改为只计算可规划的城内/城外格，固定主基地不再重复计入；每次升本分别新增 4 个城内格和 4 个城外格（合计 8 格），并补充逐级回归测试。
- 清理配置中心遗留的主基地 5–10 级行，并让发布合并器固定保留规范名称“主基地”，避免共享 CSV 的旧“城镇中心”名称覆盖新版配置。
- 人口超过硬上限后按 1 倍至配置的归零倍数线性削减繁荣额外加成，达到默认 2 倍时额外加成归零但基础产值保持不变；客户端显示归零阈值并补充回归测试。
- 配置中心显示酒馆支线刷新概率时，对历史共享 CSV 的空值回退到运行时默认 `0.5`；发布合并器不再用空字段覆盖新的非空默认值，同时保留明确填写的 `0` 关闭刷新。
- 发布合并器兼容旧版十级城镇中心 CSV：旧共享表中的 `main.maxLevel=10` 不再覆盖新版主基地四级上限，避免配置迁移误报槽位缺级并阻断发布。
- 部署迁移兼容主基地改为四级后遗留的 `balance_overrides.json` 中 `main|5..10` 旧行：仅归档已删除行，保留并应用仍有效的覆盖，避免旧数据阻断发布。
- 发布通道为长时间的 legacy-SCP 上传加入 SSH keepalive，避免公网链路短暂空闲或丢包时提前断开而导致发布回滚。

### 变更

- 王国页新增随滚动保留的当前村庄指挥条，集中显示当前村、切换入口与所属村庄五项资源；工作区单开时自动使用全宽，双开时保持左右并排，避免展开后半屏空置。
- 王国页指挥条标题改为整行对齐，村庄卡与资源卡统一基线；工作区展开保持原双列位置并在卡片下方直接展示完整内容，移除“前往完整军务”跳转，进行中区域改为列出当前村庄任务。
- 主基地改为 1–4 级发展阶段并同步建筑、科技、槽位、迁移和地图图标；旧城镇中心存档按现有建筑容量懒迁移到可容纳的最低阶段，主基地不能被攻城拆除或手动拆除。

### 变更

- 王国首页首屏改为“任务 → 当前村资源 → 进行中 → 两个折叠工作区”：建筑与城务、军务工作区默认收起并按玩家/村庄记忆展开偏好；完整建造、训练、驻军、援军、防御、解散、人口与宝物操作仍全部保留。工作区使用明确的展开/收起按钮、箭头、键盘焦点与行动说明，建筑和兵种卡补齐可见的详情入口提示。
- 繁荣度改为基础值之上的额外加成：劳动人口占总人口达到 70% 时资源产量、建造、练兵和研究耗时获得默认 +30%，降至本族动员上限对应的最低劳动占比时仅取消额外加成，不降低基础产值；科研点概率与判定间隔改按含士兵的总人口计算。
- 客户端王国页重构为“帝国指挥桌面”：以整体任务、当前村资源、进行中事项、发展工作区与军务摘要分层呈现；完整训练、驻军、援军、防御和解散能力收纳在可展开军务档案与军队页，不删除既有操作。
- 顶栏移除王国资源合计及其客户端刷新请求，只保留当前操作村选择器与全局声望；资源账本明确只显示所属村库存。
- 地图选择己方村庄和左侧村庄索引改为纯定位/选中，必须通过目标卡“确认切换为当前村庄”才调用切村；确认后继续停在地图，只有当前村可进入王国管理。
- 科技页改为“当前研究 → 下一步决策 → 完整科技树”，战报页明确为战争纪要时间线。

### 新增

- 新增主线 m10「向前一步」与任务线「开眼看世界」入口 m11：主基地二级门槛、累计探索 200 格，以及 m10 的四资源产量 +25%（12 小时）和 m11 的联盟大厅/议会厅解锁奖励；新增 `buildings.csv.maxCount` 每村建造上限列，联盟大厅与议会厅均为主基地二级、每村限建 1 座。
- 宝库升级扩容不再自动把主基地宝物搬入宝库；既有宝物位置保持不变，新宝物和备用栏装载继续按主基地优先规则落位。

- 酒馆任务槽支持按槽位独立概率刷新酒馆支线：新增 `building_levels.csv` 的 `taskSideQuestChance` 酒馆参数，默认 0.5；未抽到支线时继续刷新日常，等级配置仍统一控制刷新间隔与槽位数。一次性支线完成、放弃或已被玩家接取后不会回到酒馆刷新池；配置中心保存新增支线时自动补齐空白接取/交付对话模板。

- 新增独立 `/config` 配置中心：平衡参数、任务定义、任务目录、任务关系图和对话编辑不再从 GM 首页进入；GM 仅管理实时 JSON/WAL 状态。配置中心保存会记录 CSV revision/哈希、写入共享配置并异步创建 GitHub 配置 PR。

- 新增旧 `balance_overrides.json` 的一次性迁移器：部署时先隔离校验并折叠进 CSV，成功后带时间戳归档；运行时不再读取或写入该覆盖文件。新增配置同步 outbox/status 元数据及 `/config/status` 审查入口。

- 新增 `GetKingdomOverview` 只读协议：返回当前玩家全部村庄的资源、容量与净产率合计，以及每村坐标、主城标记和对应快照；不包含声望，也不新增持久化状态。
- 客户端重构为“王国 / 地图 / 科技 / 战报”四个一级入口：顶栏区分王国资源合计与全局声望，王国页集中当前村资源、建设与完整军务；地图新增己方村庄空间索引和进入王国管理入口。

### 变更

- 战报历史现仅保留战斗结算与侦察报告；建造、训练、行军、宝物等事件仍照常实时驱动各自界面，但不再写入或显示在报告页。旧存档中的非战斗/侦察通知会在读取时自动清理。

- 重构任务 owner：`task` 状态归一化、玩家归属目录与任务图运行时目录拆入 `modules/task/`。任务模块不再由 `app.ts` 直读玩家集合，而是通过 `player.*` Command 获取只读归属快照；既有任务 action、存档字段与六张 CSV 配置保持不变。任务图目录把节点、条件、目标、效果和关系边与旧 `QuestDef` 兼容投影集中，GM 仍以 CSV 为唯一事实源。

- 任务测试将 S4 释放领奖链路从不可达断言拆为独立真实流程，架构守卫递归检查同一 owner 的内部子模块，防止今后拆分绕过跨模块 import、定时器和状态归属约束。

### 修复

- 发布器不再用生产共享 CSV 整文件遮蔽 Git 新配置：按各表稳定主键合并，保留已有 GM 手调值并自动带入新增行/列，已删除的旧行不会在后续发布中复活。

- 恢复 S3 接取后追问为独立 `s3_after_accept` 对话对象：GM 文本编辑器中使用独立 id/code，只有任务接受成功后才弹出。

- S3 接取对话恢复“接受后”语义：`s3_accept` 第 2 段仅在玩家点击接受任务后展示，关闭或选择离开不会提前推进到该段。

- 对话配置中心表格改为按稳定 `code` 自动排序，同一对话对象的多段文本再按段落号排列；加载、新增/删除段落和保存都会保持该顺序，便于查找和审查。

- 合并任务对话默认分支：M8 成功交付、M9 按 M8 成功时的接取/交付文本分别并入 `m8_deliver`、`m9_accept`、`m9_deliver`，移除成功分支 entries；S3 接取后追问并入 `s3_accept` 第 2 段并移除 `s3_after_accept`，运行时仅对失败结局使用 `*_failure` 触发器。

- 恢复配置中心对话目录中曾由服务器共享配置保存的 M7、M8、M9 接取/交付及 M8 结局分支文本，保留空白模板与既有稳定对话代码；配置中心首页将平衡、任务和对话入口移至页面顶部，打开后即可直接进入编辑器。

- 修复宝物入栏优先级：新获得、返程/跨村交付和炼金炉收获的宝物，以及从备用栏装载的宝物，现在在城镇中心与宝库主栏都有空位时优先进入城镇中心；宝库扩容时既有宝物的迁移规则保持不变。

- 修复部署迁移遇到已删除历史常量 `treasure_trade_drop_chance` 或旧科技字段 `research.effectValue` 时整批配置被阻断的问题：废弃项现在只在原始备份中保留并记录，其他有效 GM 覆盖仍会安全折叠进 CSV；未知表/行/字段继续硬失败。

- 调整地图行军旗帜与阵营圆底的视觉重心：圆底居中于旗帜主体而非旗杆脚点，避免二维叠加时产生“旗杆插入圆心”的错位感。

- 修复 GM 重置本村任务时遗漏 `taskVillages` 任务实体的问题：M8 天王老子村不再因稳定 ID 复用而继承已清空的守军/库存；重置会等待 PvE 地块移除，启动恢复也会修复旧版 active M8 的空营地并按当前 GM/CSV 模板重建。

- PvE 掠夺装载改为金币优先；金币装载完后，剩余运力在木材、泥土、钢、粮食之间尽量平均分配，并继续严格受运力上限约束。

- 修复 PvE 战斗守军快照复用存档对象导致的重复扣减：战斗过程中对快照的逐 tick 修改不再提前写入营地状态，失败战斗会正确保留幸存守军。

- 调整地图行军旗帜与阵营圆底的视觉重心：圆底居中于旗帜主体而非旗杆脚点，避免二维叠加时产生“旗杆插入圆心”的错位感。

- 修复行军编队返回上一步后无法重新选择行军模式的问题；村民请求仅在清理常驻 PvE 营地后按概率触发，任务临时营地不会触发；PvE 战利品在运力足够时可完整搬空并始终受运力上限约束。
- 修正 M8 天王老子村：模板与新生成任务村均使用配置中心 CSV 的四种资源各 500、金币 500；守军改为条顿 `clubswinger` 棍棒兵，移除错误的 `club` 标签。
- 解散军队折叠菜单改用与建筑管理一致的右侧小箭头，并在展开时旋转指示状态。
- M9 分支对话稳定代码改为 `m9_accept_m8_success`、`m9_accept_m8_failure`、`m9_deliver_m8_success`、`m9_deliver_m8_failure`，任务模块继续按 M8 结局正确调用。
- GM 重新触发已完成 M8 时清理上一轮战后的天王老子村，重新接取后按当前 CSV/GM 模板恢复 15 名条顿棍棒兵、四种资源各 500 和 500 金币。
- 修正天王老子村旧存档缺少 `task` 标记时仍走普通 PvE 掉落的问题；M8/M9 清理不会直接掉落铁壁勋章，铁壁勋章只能在任务奖励领取时发放，并为并发领取增加互斥，避免同一任务重复领奖。
- 修复天王老子村 NPC 攻城预警的地图图标停在首次出现位置的问题：受袭村现在会消费定向 `MarchStep` 增量，实时更新来袭军当前位置与插值动画。
- 修复来袭预警渲染态缺少 `marching` 导致 rAF 不做逐格间插的问题；M8 攻城图标现在会在服务器步进之间连续移动。雇佣兵营地配置统一为城外建筑。

- 失败任务不再在战斗结算后从任务栏消失：M8 守城失败与 S3 失败会保留为“任务失败”，玩家可在任务卡确认失败；确认后才发放配置的失败奖励并触发失败对话。M8 确认失败会保留结局并解锁 M9，M9 按 M8 成功/失败选择对应奖励；任务卡不再把 M8 失败显示为可领奖。
- 恢复军队页“解散军队”默认折叠菜单的展开箭头，明确提示该区域可展开。

- 允许冒险者从来袭预警中执行途中侦察；冒险者仍不参与侦察战，也不能担任防守侦察兵。

### 新增

- 新增主线 M7「我们需要进步」、M8「冤家路窄」和 M9「反击」：M7 检查学院与科技研发并奖励 3 科技点；M8 接取后按 GM 倒计时生成天王老子村并发动 NPC 攻城，成功/失败均等待手动领奖；M9 掠夺该任务村后按 M8 结局发放人口或铁壁勋章，任务村在 M8 战后保留、M9 成功后移除。新增可按结局配置的任务对话与奖励效果。
- 新增城外探险家协会与通用兵种冒险者：可探索和执行侦察、战斗力为 0、不会发现侦察部队，被真实侦察兵发现时全军覆没；侦察/返程继续保持地图隐身且不参与遭遇战与伏击战。冒险者建筑、兵种、M8 任务村守军和倒计时参数均来自配置中心 CSV。

- 主线奖励更新：M3 交付奖励改为 5 名平民人口；M5 交付奖励改为人口增长速率 +10%、持续 24 小时的临时加成，并移除两者旧的金币/宝物奖励。两项效果均通过任务效果 CSV 配置并在任务卡/领奖弹窗显示。新增 `population.GrantPopulation` 与 `population.ApplyTaskGrowthBuff`，临时加成到期由 Scheduler 清理，重启可恢复剩余有效期。GM 任务模块编辑器同步提示效果参数格式。

- M1 增加隐藏 success 兜底条件：当村庄不存在仍需修复到 1 级的资源田时，即可与原有“四块资源田均已修复”条件二选一交付；该条件不写入玩家任务描述，仅保留在 GM 任务图供审查。

- 对话编辑支持同一对象的多段有序文本：GM 可为指定 `id` 追加段落并只编辑 NPC 名称、文本和回复；触发后关闭或选择会自动进入下一段，最后一段结束。旧单段对话兼容，无需刷档。

- 负声望新增军队攻防倍率 buff（每点与上限均可在 GM 声望参数中独立调整），作用于出征、守城和掠夺防守；新增 `task.GmResetAll` 任务专项重置命令，仅清空任务状态/任务营地并重新激活各玩家主城 M1；日常任务不再计入任务提醒，非日常三级任务展开时仍显示可接取提醒。

- 主线任务更新为 M1「初来乍到」、M2「大兴土木」、M3「人丁兴旺」、M4「排除威胁」，并新增 M5「那边有什么？」（累计探索 100 格，含初始城镇视野）和 M6「国库充盈」（主城拥有 100 金币）。新增 `build_buildings`、`population_reached`、`resource_owned`、`explore_tiles` 声明式目标，进度由建筑、人口、资源和战争迷雾权威模块实时同步；M4 接取时按 `rats:1` 在接取村附近生成老鼠窝强度任务营地。
- 为每个主线预置空白接取/交付对话模板；配置校验要求以后新增主线同时提供两行。交付对话与任务奖励一并返回并在领奖弹窗展示，M1 既有对话文本不变。GM 任务编辑提示同步列出四种新目标及参数格式。

- 新村开局模板统一提供 1 级城镇中心、1 级集结点和四块降至 0 级的受损资源田（木材、泥土、铁矿、粮田），资源田可分别修复至 1 级；主线 M1 改为修复这四块资源田，修复进度和奖励目标会在任务卡片显示。
- 新增任务绑定的 NPC 对话 session：任务接取前可由 `dialogues.csv` 配置 NPC 名称、文本和回复选项；S3“村民的请求”现会显示幸福村村民的求助对话，选择“离开”只关闭对话，选择“接受任务”才正式接取且之后仍可再次打开。
- 配置中心新增“对话编辑”，可事务校验并热重载 `dialogues.csv`，支持新增、删除和修改对话文本及玩家回复。
- 为全部主线与支线任务预置可在 GM 填写的接取对话模板；主线自动激活时、支线接取后可配置一次性对话。S3 接取后新增当前接取村庄名的动态村民质疑对话，仅提供默认关闭键；新账号首次进入时 M1 的自动接取对话也会从任务状态弹出。
- S4 带回“被囚禁的娜塔莉们”后，报告页待处理宝物卡片新增“查看对话”入口；对话对象和文本按 `dialogues.csv` 配置，只有关闭键，不改变释放/入库的任务抉择。

### 修复

- 修复侦察任务全灭时不生成进攻方战报的问题：进攻方收到“侦察战失败/全灭”及逐兵种战损，防守方收到独立的侦察战报告（来袭侦察兵与死亡数），不再误显示为自己的侦察情报；失败时不泄露未取得的守军快照。

- 修复主动侦察战报：按兵种记录进攻方战损并向守方正确发送反侦察报告；冒险者可主动侦察但不参加侦察战，不能执行途中侦察或担任防守侦察兵，遇真实侦察兵时全军覆没。补充无守方侦察兵、冒险者全灭及防守限制回归测试。

- 修复主线 M3 人口门槛漏算驻军/训练中士兵；恢复军队页底部默认折叠的“解散军队”管理区，解散驻军会正确返还其人口。
- 修复旧存档天王老子村资源未迁移到当前 GM/CSV 默认值的问题：未发生战斗的旧任务村会惰性迁移为四种资源各 500、金币 500，并持久化迁移标记；战后资源和兵力不会被回滚。
- 修复 M8 攻城调度的重启恢复与失败重试：旧任务村缺少攻城时间时会补调度，发送成功只生成一次 NPC 攻城行军，受袭村会收到地图路线和视野内来袭预警，抵达后正常创建战斗。
- 修复 PvP 侦察报告把守军误显示为“无”：侦察改读目标村实际驻军，不受“防御掠夺”开关和兵力配置影响；GM 任务管理按真实任务 code 渲染 active 数组，主线回退不再发送数组下标导致 `unknown_quest`。
- 修复 M8 天王老子村坐标不同步：侦察、掠夺和任务地图标记现在以 World 中同一 `refId` 的地块为权威，并回写旧存档中的 PvE 坐标；玩家在 NPC 攻城前主动清空任务村也会判定 M8 成功，任务村保留并按战后规则减半资源、清零金币。M8 任务村默认四种资源各 500、金币 500。
- GM 任务管理新增主线状态操作：已完成主线可重新触发（M1 按自动激活规则，其余回到可接取），进行中的主线可回退为未触发并清理其任务实体。

- 移除建筑卡片上的直接“拆除建筑”按钮；拆除统一收纳到建筑详情页默认折叠的“建筑管理 → 拆除与移除”菜单，特殊建筑弹窗也复用同一入口，避免误触并保证入口一致。

- 修复生产 `current/data` 符号链接下 GM 配置镜像路径错误：CSV 现在解析到共享数据目录的同级 `shared/config`，后续 release 的配置覆盖可稳定生效，不会误写入 `shared/data/config`。
- 修复 GM 调参只停留在 `balance_overrides.json`、后续 release 被仓库 CSV 覆盖的问题：平衡表、任务模块、任务目录和对话编辑保存后均写回 CSV，并镜像到生产共享配置，由发布脚本按 manifest 在删档、重启和后续部署时恢复；GM 平衡面板补齐议会厅服务、PvE 目标/守军等表的编辑入口。
- 补齐所有非城镇中心建筑卡片的“拆除建筑”入口。M2 按接取后的 1 级新建完成事件计数；接取前已占用但在任务期间完整拆除、重新形成空槽的槽位，后续新建可计数，因此不会因接取时槽位已满而卡死；同一槽位已计数后重复拆建不重复计数，升级、修复和接取前已在建造队列中的建筑不计数。

- 修复 M2 进度回退到“当前建筑总数差值”的兼容路径：新任务从 0 开始记录建造完成次数，并记录拆除释放的初始槽位；GM 平衡面板新增独立的 M8「任务村参数」区，可直接调整 `m8_attack_delay_sec`（默认 28800 秒/8 小时）。
- 修复任务营地掉落串线：普通任务营地不按通用 `droprate` 产出宝物，也不生成其它任务的专属宝物；仅 S4 最终清理阶段显式生成“被囚禁的娜塔莉们”。

- 恢复今早首次发布前（`8d8307a` release）由 GM 任务模块/对话编辑器保存的配置：M1/M2/M4/M6 奖励、主线前置关系以及 M1–M6 接取回复和 M1 交付文本；仅更新 `config/quest_effects.csv`、`config/quest_edges.csv`、`config/dialogues.csv`，不回滚玩家存档、不删档。

- 修复地图行军路线在部队抵达驻扎点后仍常驻显示的问题；已探索地形不再被黑雾遮罩为不可辨识色块，未探索格仍保持战争迷雾保护；新增无圆环立体战旗地图标记并按敌我与行军类型做视觉区分。
- 修复地图已探索但当前失去视野时的轻雾层被误删问题；保留地形辨识度的同时恢复探索记忆遮罩，并为己方/敌方战旗增加绿色/红色圆形阵营底座以提升地图可见性。

- 恢复上次部署前通过 GM 对话编辑器保存的主线任务接取/交付文本，并迁移为新的 `segment=1` 格式；不涉及玩家存档或删档。

### 破坏性

- 外部任务动作新增 `task.StartAccept`，Wire 升级至 v10（最低兼容 v10）；客户端与服务端需同步升级。对话 session 不落盘，无需刷档。

### 变更

- 村庄页改为列表化指挥台：移除首屏村庄场景图，顶部聚焦当前任务并提供紧凑村庄切换器，下方按左侧指挥台/人口/宝物、右侧建筑清单重排，并适配手机端单列浏览。

- 主线任务改为仅 M1 建村自动激活；后续主线满足前置后进入任务页的可接取提示，玩家手动接取后才开始执行。任务页按全局/村庄一级菜单、任务类型二级菜单和具体任务三级菜单显示可接受任务，并在折叠菜单摘要显示提示数量；各级菜单展开状态按玩家持久化，重新进入任务页时恢复。

- 王国任务完成后进入“待领取”状态并冻结有效期，只有玩家领取声望奖励后才重新开始下一轮倒计时；任务奖励、任务专属宝物和其他送达/转交宝物不再设置报告过期时间，只有普通 PvE 营地的随机掉落保留倒计时。

- 军队页“防御掠夺”按来源拆分兵力：本村驻军与每支玩家/王国援军独立显示、独立开关和数量配置；战斗结算按实际来源扣除伤亡，不再把相同兵种混成一个兵力池。协议升级至 Wire v9（最低兼容 v9），旧行军记录按缺省全军参与兼容，无需刷档。

- 军队页训练中心恢复为左右分栏：左侧切换军事建筑并显示空闲/训练中状态，右侧仅列出当前建筑可训练的兵种；训练请求继续绑定所选建筑，训练队列保留建筑来源标识。

- 行军/军队协议升级至 Wire v8（最低兼容 v8）：侦察返程继续保持地图隐身；军队快照新增训练建筑选择/训练建筑状态、训练队列建筑来源和增援来源（村庄/玩家）信息。v8 客户端可省略训练建筑字段，服务端继续使用默认选址；无需刷档。

### 修复

- 恢复 M1「初来乍到」新账号首次自动弹出的历史接取对话；对话对象继续使用当前村庄名动态替换，M1 交付对话仍保留为空白模板。该配置修复不改变存档结构，无需刷档。

- 任务全量重置现在按 `pve` 目录扫描并移除任务营地，兼容任务状态与营地脱节的旧存档，避免重置后地图残留任务营地。

- 修复真实存档中极端战斗把最多 20,000 个逐轮快照写入单条战报、导致登录后历史报告占满同村串行队列并使人口/声望/建筑长期无法加载的问题：战报保留结算摘要、总轮数和最多 120 个均匀关键轮次，启动时自动无损压缩旧战报；不删战报、不刷档。

- 按引入 NPC 对话前的稳定刷新链路重建客户端加载：登录一次性拉齐村庄快照并预取历史报告，切村只执行一轮明确刷新，移除会持续合并排队、按页面重复补拉及后台覆盖缓存的四轮加载改造；NPC 对话、任务提醒和侦察军不参与遭遇战/伏击战的后续功能保留。

- 修复侦察军误触发普通遭遇战/伏击战的问题：侦察军及其返程持续隐身，不会出现在他国地图，也不会参与野战；启动时会自动解除旧版本已产生的侦察野战，让双方保留当前兵力并从当前位置返村。GM 另提供 `POST /gm/ops/cancel-scout-encounters` 用于立即清理存量错误战场。
- 修复游戏卡顿：旧侦察遭遇战因侦察兵攻击快照为零而持续空转，现会被启动清理和 GM 运维命令终止。
- 任务页签在有可接受支线任务时显示数量提醒，数字口径仅统计 `offeredSide`，与报告页待处理提醒一致。

- 修复增援地图实时更新：玩家/王国增援的 `MarchSent`、逐格 `MarchStep`、抵达驻防和到期移除事件现在同时定向到目标村；王国增援的内部伪出发村不再导致目标玩家只看到初始位置、军队在地图上卡住。

- 地图战争迷雾：加深已探索格子的灰雾层，增强与实时可见格子的视觉区分。

- 侦察隐蔽性：侦察行军不再作为目标方的来袭行军或地图外军出现，轮询与增量推送均过滤主动侦察和途中拦截侦察；军队页恢复“防御掠夺”配置，可逐兵种设置数量、关闭防守并保存到服务端。

### 破坏性

- 行军协议升级至 Wire v7（最低兼容 v7）：`Movement` 增加临时增援模式及其驻防到期字段；客户端与服务端需同步升级。旧存档中的行军记录通过可选字段兼容，无需刷档。

- 来袭预警与途中侦察协议升级至 Wire v6（最低兼容 v6）：`ListMovements` 新增实时派生的 `incomingWarnings`，敌方来袭不再作为完整 `movements` 下发；新增 `SendIncomingScout` 与 `IncomingWarningChanged`，旧 `IncomingAttack` 通知停止产生且不再写入战报。旧存档只使用可选行军字段，兼容读取，无需刷档。

- 军队训练协议升级至 Wire v5（最低兼容 v5）：`TrainTroops` 不再接受建筑 `slotId`，由 Military 服务端按空闲最高等级、同级稳定 `slotId` 选择训练队列；`GetArmy` 以统一 `trainable` 投影及无建筑语义的 `trainingQueues` 替代 `slots`/`training`，`CancelTraining` 改用 `queueId`。

### 新增

- 王都与四象限封地作为公开地图地标，即使未探索也显示地标轮廓；炼金炉管理弹窗新增拆除建筑按钮。王国清理 PvE 任务在进行中或待领取时提供与普通清理任务一致的“前往地图”定位按钮。

- 议会厅增援改为王国控制的临时驻防：增援抵达后不并入目标村常驻军队、不可由玩家下令，按 GM 可调时长自动撤离；玩家间增援同样只移动军队位置，口粮、人口和伤亡归来源村，目标村战斗结算优先扣除临时增援。增援兵力配置显著高于同声望价格的代打服务。

- 玩家间增援抵达后可由出发村召回，目标村只显示来援部队；召回继续沿当前位置到来源村的剩余路程返程，避免误显示为普通驻军。

- 新增实时来袭预警与途中侦察：仅当掠夺/攻城来袭军当前处于目标玩家任一城池或军队视野内时，村庄、军队和地图页显示倒计时、出发村庄与完整行军路径；失去视野后预警立即消失，默认隐藏兵种、规模和宝物且不进入战报。玩家可从预警派侦察兵沿来袭路径相会，按普通反侦察准则由来袭军中的守方侦察兵杀伤进攻侦察兵，且不改变来袭军的路径和 ETA；本方有幸存侦察兵即更新兵力情报，零损失且至少一名存活的完胜额外识别携带宝物；来袭军无侦察兵时不会发现该行动。旧存档通过可选字段兼容，无需刷档。

- 登录页新增主服与 AI 测试服 01 的选服入口；切换会跳转到对应的 8080 / 8081 世界，使 WebSocket 连接、登录会话和存档自然隔离。

- 新增王国系统：世界中心王都与四象限封地作为强守军 PvE 地标；玩家按出生象限固定效忠封地，循环接收有期限、无失败惩罚的全局王国任务，并在手动领取时获得声望。新增 1–5 级城内建筑议会厅，可用声望购买分级物资、增援、宝物与王国军代打；任务权重、期限、目标量、奖励、地标守军和全部服务数值均可在 GM 修改并写回 CSV。旧账号惰性建立王国状态，无需刷档。

- 新世界使用 96×96 确定性环面地图，生成连续斑块式平原/森林/丘陵（55/30/15）、至少 550 个间距不小于 4 的首村槽位，并在保留人工锚点的基础上将公共 PvE 补足到地图面积的 5%；旧世界继续使用既有 `world_meta` 尺寸，无需刷档。

- 地图格快照新增可选 `terrain`；只在已探索或实时可见格下发，未探索格不泄露真实地貌。协议版本升至 5，服务端继续兼容版本 4。

- 新增自动探索行军：可向未探索终点派出部队，逐格发现公共 PvE 营地、其他玩家城池或非己方部队即返程；抵达终点也会返城，且不会主动战斗或驻扎。自动探索的首次揭示回执持久化，重启后不会漏掉本步遭遇判定。旧存档通过可选字段与惰性创建回执集合兼容，无需刷档。

### 变更

- 生产公网冒烟对 HTTP 静态资源请求加入有限重试，并将公网单请求超时放宽至 30 秒；服务器链路瞬时丢包或低速时不再因一次 `fetch failed`/10 秒超时误判新 release 故障，本地冒烟仍保持快速失败。

- PvE 目标主表与守军表纳入 GM 平衡覆盖及 CSV 写回；王国代打军复用正常战斗结算，但不占玩家人口、不触发医院伤亡回收或 PvE 宝物掉落，目标在出发前失效会自动退款。

- 玩家注册改由 World owner 原子分配并占用首村槽位，世界满员时返回 `world_capacity_exhausted`，不再回退到 `(0,0)`；PvE T1–T8 重生调整为 15 分钟至 24 小时。

- 新增独立 AI 测试服 01：PM2 进程 `kow-test-01` 监听 8081，使用隔离的 `data/test-01/game.json` 与日志；发布与回滚流程会同时重建并健康检查主服和测试服。

- 战斗战报现在保存双方开战阵容与完整逐轮回放；战报页仅允许战斗报告展开，展开后可查看每轮双方损失和轮末剩余兵力，其他动态保持静态卡片。

### 修复

- 修复旧存档已探索区域缺少地形字段时全部回退为平原的问题：服务端从确定性世界地貌补齐旧快照；地图空闲格按底层地貌显示为“平原 / 森林 / 丘陵”，不再统一标作“空地”。

- 补齐地图生成边界：每个 96×96 首村槽都具备半径 4 内两个老鼠窝及半径 6 内一个野狼群，PvE 生成遵循森林/丘陵生态偏好并在旧世界坐标碰撞时安全换点；respawn 统一通过 World 原子槽位重排到 96×96，season 保持旧坐标；普通拓荒不可占用备用首村槽，全图请求支持半径 96。

- 修复伏击战报的双方视角与参战数量：被伏击方现在标记为真实的防守阵营，显示自己派出的兵种与自身损失，并按伏击战客观结果显示胜负；显式出征兵力不再被错误钳制为城内剩余同兵种数量，避免战报和实际战力少算。

- 地图现在按玩家级快照展示所有己方村庄派出的军队、实时位置和路径；点击其他村庄的军队会先提示切换到来源村庄，再允许下达命令，避免切村后看不到部队或把命令发给错误村庄。

- 明确并回归覆盖地图视野的玩家维度：切换当前村庄后，`GetArea` 仍使用会话 `playerId` 聚合所有己方城池与军队视野，不会只显示当前村派出的军队视野。

- 修复伏击军续行：已驻扎的伏击军选择下一处空地后继续以 `ambush` 模式行军并在新地点伏击，不再错误转换为普通驻扎；到 PvE 或玩家村庄时仍按目标类型执行掠夺/攻城。

- 村庄切换现在显示全局“正在切换村庄…”遮罩并锁定页面操作，直到目标村庄的核心快照加载完成；非地图页切换时跳过整张地图请求，进入地图后再补拉，减少切村等待和误操作旧村数据。

- 修复任务营地坐标分裂：全局任务营地以激活/接取时村庄坐标为唯一快照，客户端切换村庄仍显示同一营地；服务器恢复时会校准历史 PvE 实体与任务快照。村庄级任务允许同一玩家其它村庄出兵，但进度、任务掉落报告和奖励仍归任务所属村庄。

- 任务页改为“全局任务 / 村庄任务 → 主线/支线/日常 → 具体任务”的三级菜单；村庄任务仅显示当前村，并在任务页提供村庄切换栏。
- 任务页自动隐藏没有内容的二级分类，三级任务默认展开并移除虚线框；同一玩家的一次性支线被一个村接取后会从其他村的列表移除。
- 资源栏村庄切换卡片现在整块可点击，修复紧凑布局下卡片下半区域无法打开村庄选择的问题。
- 修复任务营地战败后的生命周期：默认战败不推进或失败任务，若实体因异常结算消失会按任务坐标自动恢复；任务营地继续保留到任务放弃或正式完成。
- 地图任务营地详情现在显示关联任务名称、任务说明、任务类型和营地清理进度；地图推送刷新时保留任务关联信息。
- 地图点击目标后直接进入“目标评估”面板，并在同一层列出服务端返回的可用行军模式；选择模式后直接进入编队，移除独立的“选择行军模式”菜单层。

### 修复

- 任务页现在拆分为“全局任务”和“村庄任务”。主线默认全局、每日任务和当前 s1–s4 支线绑定村庄；全局任务以主城为状态锚点但记录最后执行村，资源/声望/宝物奖励及报告待领取物会投递到该执行村，切换村庄后村庄区即时更新。

- 行军目标现在以 World 的权威村庄坐标和名称校准前端缓存；同一坐标的己方村庄不会显示或执行 0 距离转移，GM 调整后地图与行军面板不再残留旧坐标。
- 地图当前村庄标签改为读取当前村的村名（不再误用玩家名）；资源栏村庄下拉改为紧凑单行高度，避免拉伸资源栏布局。
- GM 修改玩家村庄坐标时现在通过 World owner 命令同步地图地块，已有存档在服务端启动时也会校准，行军目标与路径不再残留旧坐标；资源栏新增全局当前村庄下拉框，所有页签都能查看并切换当前操作村。

- 地图选中当前操作村时不再提供“转移”或重复“切换到此村”按钮；移除玩家地图上的“放弃此村”入口，保留服务端内部清理命令供兼容与测试使用。

- 建筑侦察战报现在同时展示城内外建筑与守军兵力；掠夺战普通部队造成的建筑破坏按被破坏等级生成并合并建筑战利品，按现有运力规则带回，避免战报显示建筑损坏但战利品为空。

- 村名重命名现在通过玩家维度事件刷新所有会话的地图区域缓存；转移宝物待处理报告标明来源村并按目的村报告归档，避免地图名称与报告归属滞后或混淆。

- 村庄、军队和科技页的村庄列表在只有一座村庄时也保持可见；旧版在村庄数小于等于 1 时整块隐藏，导致玩家误以为村庄切换功能未生效。兼容缺少 `villages` 数组的旧登录响应时回退显示当前村庄。

- 移除拓荒 `found_min_soft_limit` 人口规模限制；拓荒仍按主基地等级、拓荒者、资源、距离和行军点校验，避免把人口容量缺口误报为人口门槛。

### 新增

- 新增“伏击”行军模式：已探索/可见空地可派军伏击，抵达后进入驻扎并将视野限制为1；敌方行军进入一格内触发伏击战，伏击方按 GM 可调攻击加成，敌方远程兵优先按近战数据承伤。战斗结束后双方幸存部队均返回各自出发村，伏击军在抵达前仍按普通行军参与野战。

- 贸易中心新增“转移资源”栏：仅列出贸易半径内的己方/盟友村，使用商队正常占用并回收贸易路线；地图“转移”行军改为只携带部队与宝物，资源不再混入军队运输。

- 村庄列表的每张村庄卡片新增“修改名称”按钮；服务端校验村庄归属与名称长度，名称持久化到玩家数据并通过事件同步地图地块，重命名分城不会切换当前操作村。

- GM 平衡面板新增“拓荒参数”专用板块，集中编辑第 2 座城每种资源的基础开城包、后续城成本增长倍率、拓荒者数量和主基地等级门槛；页面同时显示四种资源成本公式与当前默认值。参数继续写入持久化平衡覆盖，删档不会清除。

- 新增独立的玩家任务页：服务端 `task.GetPlayerState` 聚合玩家全部村庄的任务，任务卡标明所属村庄，执行操作前自动切换到来源村；村庄页不再嵌入任务栏。村庄、军队和科技页新增可复用的村庄列表，切换后刷新对应村庄建筑、兵力或科技树。

- 科技树按村庄工作区切换；`scope=player` 的全局科技完成集合跨村计算，已在任意村研究完成的科技在其他村显示已完成并拒绝重复研发。

- 训练队列增加“取消训练”按钮：取消后停止该建筑实例的队列，尚未产出的兵力返还预留人口与训练资源，已产出的兵力不受影响。

- PvP 攻击分为掠夺与攻城：掠夺可由守方配置防守兵力且不启用城防加成，攻城启用城墙/宝物/科技并允许攻城武器破坏城内建筑；战后按幸存战力从高等级建筑逐级拆除，攻城另可掠夺仓库/粮仓存量。GM 全局常量新增拆除阈值与仓储掠夺比例，战报补充战斗类型、建筑损坏、建筑收益和存量收益。

- **[需刷档]** 新增城内建筑炼金炉：可放入三件相同品质宝物，按更高品质目录 `dropRate` 加权炼化，完成后在炉内等待收获；新增主/备用宝物栏选择、炼化进度、满栏保留结果提示及服务端持久化调度。存档结构版本升至 5。

- 任务卡片现在展示完成、失败及多阶段分支的资源、宝物和声望结果；娜塔莉宝物的 `-1/-2` 声望扣除移回 S3/S4 对应任务结算，宝物本身不再提供被动声望。

- 新增玩家声望模块：初始值为 0，S4 释放娜塔莉们和符合正负声望门槛的 PvP 击杀会调整声望；正声望提高人口增长但降低金币税收，负声望降低人口增长但提高 PvE 营地宝物掉落概率。资源栏新增声望值摘要和效果悬浮说明，GM 平衡面板新增声望行为、门槛与效果参数板块。

- 地图行军模式统一为目标驱动：空地显示驻扎（拥有拓荒者时额外显示拓荒）、PvE 营地显示掠夺、己方村庄显示转移、盟军村庄显示增援、中立村庄显示增援或掠夺/攻城并宣战、敌对村庄显示掠夺/攻城、未探索地块显示探索。新增外交 owner 保存盟军/中立/敌对三态关系（默认中立），服务端权威返回目标模式并在中立玩家确认攻击时执行宣战；确认页补充预计行军时长、可派兵力、行军点/集结点等级和宣战提醒。

- 新增城内保险库建筑（最高 10 级）：`building_levels.csv` 为每级配置木材、泥土、铁、粮食和金币的增量保护量，GM 建筑卡片可编辑并通过持久化平衡覆盖保存，删档不会清除；村庄建筑卡片显示当前等级累计保护量。攻城结算先按幸存攻城武器拆除建筑，再读取拆除后的保险库等级扣除保护额，最后装载仓库/粮仓战利品；保险库被拆后同一场攻城立即暴露更多资源。

- 新增侦察行军：敌对或中立玩家村庄提供“侦察”模式，只允许三族侦察兵，支持携带宝物；可选择 `scout_resources`（资源与守军）或 `scout_buildings`（城镇中心、城内、城外建筑种类与等级）报告。幸存侦察兵带宝物原路返城，全军覆没时宝物转入防守方待处理报告；侦察报告按 Travian 式“进攻方有幸存者才收到、守方击落侦察兵才收到”推送。

- PvE 营地也提供“侦察”模式；PvE 目标仅可选择资源与守军报告，服务端禁止建筑侦察，并复用 PvP 侦察的反侦察战斗规则。

### 变更

- 地图改为消费服务端权威的平原、森林与丘陵分类，并用聚合 SVG 色块、跨格树冠和山脊绘制连续地貌；地块贴图与 POI 地形替换已移除，默认隐藏蜂窝格线，未探索区不再暴露地形，旧响应缺少地形时安全降级为平原。

- 地图定位按钮由“回主城”改为“回正并回当前村”，目标面板在已经处于当前村时不再显示“切换到此村”；转移行军明确提示不携带物资，资源转运统一走贸易中心。

- PvP 建筑结算区分“破坏”和“拆除”：普通部队只破坏城外建筑等级并保留可修复状态，防守方可按当前等级到首次受损前等级的累计资源、累计时间三分之一一次性修复；受损建筑不能升级但仍可手动拆除。攻城武器先于普通部队结算拆除，降至 0 级时完全移除并产生建筑拆除战利品；攻城时城内建筑只接受攻城武器拆除，已被普通部队打到 0 级的建筑空壳也可由攻城武器彻底移除。战报明确标记每条建筑的破坏/拆除及等级变化，并注明建筑是否被移除。

- 移除兵种配置和运行时的 `popPermanent` 参数。所有兵种的人口占用只由 `popCost` 决定，返程、解散和医院回收不再跳过拓荒者；拓荒建城默认消耗 1 名、占用 5 人口的拓荒者，成功后出发城减少这 5 人口，新城以 5 人口初始化。无需刷档，旧存档读取时按现有兵力和 `popCost` 惰性计算。

- 炼金炉最高等级固定为 1 级；GM 平衡面板将 `alchemy_refine_sec` 炼化时间参数合并显示在炼金炉升级消耗卡片内，支持持久化修改并热重载，无需刷档。

- PvP 攻城/掠夺战利品装载改为金币优先、木/泥/铁/粮尽量平均；攻城时四种基础资源优先装载仓库/粮仓掠夺物，剩余运力再装建筑拆除收益。

- 地图目标面板改为同层展示专用行军模式：不再自动跳过单一模式，也移除“继续”按钮；驻扎、探索、掠夺、转移、增援、攻城和拓荒均以明确的行为按钮进入编队与确认流程。驻扎军的下一道命令统一标为选择行军模式。

### 破坏性

- **[需刷档]** 任务定义改为可审查的任务图：`quest_lines.csv`、任务节点、条件、目标、效果、关系边六张表共同成为事实源，GM 新增模块化编辑器与关系图；任务实例新增任务线/定义修订记录，存档结构版本升至 4。

- **[需刷档]** 移除铁匠锻造状态、`UpgradeSmithy` 协议动作与旧攻防常量；军事科技改为村庄作用域的近战攻击/防御、远程攻击/防御、行军速度五组三级节点。行军速度经军事模块最终快照在出发、续行与返程时锁定；协议版本升至 4，存档结构版本升至 3。

### 新增

- 行军系统全面改进（Wire v4，最低兼容 v4，旧客户端强制刷新）：共享 `Movement`/`ForeignArmy` 类型；新增 `RecallMarch` 主动撤回在途军队；地图右上角行军面板按每支部队状态显示撤回、召回或续行操作（`found` 类型撤回会 forfeits 开城包，需确认）；来袭告警改为部队进入守方视野后触发（不再出征瞬间通知）；`ListForeign` 脱敏视图改为仅 `pos`+`heading`（隐藏完整路径与目的地）；行军逐格增量推送（`MarchStep`/`MarchRemoved`/`ForeignArmyStep`/`ForeignArmyRemoved`）替代 3.5s 外国军队轮询；空间索引优化同格相遇检测；途中相遇战统一走 Combat 模块逐 tick 结算（双方均可收到 `BattleStarted`/`BattleTick`）。

### 变更

- 宝库 GM 平衡面板明确显示「每级主/备用槽」字段；修改某个宝库等级的 `treasureSlots` 会同时调整该等级新增的主宝物栏与备用宝物栏容量，并持久化为平衡覆盖。

- 宝库宝物栏重做为主宝物栏与备用宝物栏：主栏宝物才提供被动效果，备用栏仅储存；新增 `treasuryReserve` 时通过惰性默认值兼容旧存档，宝物存入优先级调整为宝库主栏 → 城镇中心 → 备用栏，本次无需刷档。

- 支线任务稳定代码统一为短编号：`villager_request` 改为 `s3`，`investigate_coords` 改为 `s4`；任务图、GM、运行时分支和测试同步更新。读取旧存档时自动迁移活动任务、完成/放弃/可接取记录和冷却键，无需再次刷档。

- `ListForeign` 响应删除 `path`/`to`/`arriveAt` 字段（破坏性协议变更，Wire v3→v4）。

### 修复

- 修复桌面地图按 SVG 固有宽高比撑高页面的问题：地图页现在严格占满当前屏幕剩余空间，不再产生纵向滚动；主城始终居中于实际可见地图，坐标跳转与缩放控件固定显示在可见区域左下角，手机端继续预留底部 Tab 栏与安全区。

- 资源栏停产提示移除“本可”措辞，停产状态下只显示产量数字。

- 修复 S4 释放被囚禁的娜塔莉们时声望提前结算的问题：释放选择阶段只记录任务分支，点击「领取奖励」后才发放 +2 声望；收纳宝物导致任务失败的 -2 声望仍在失败结算时生效。

- 修复宝库管理弹窗空栏不可见：现在始终显示主宝物栏与备用宝物栏的容量、已用数和空槽占位，备用栏即使为空也会明确显示。

- 修复行军操控规则：商队不再显示或接受地图面板的停止/撤回命令；军队移除停止命令，且仅在派出后 90 秒内允许撤回，服务端与客户端同时执行窗口校验。

- 修复「正直的心」科技页显示基础判定间隔的问题：`research.GetState` 现在下发宝物倍率后的实际间隔，页面显示与后台科研调度一致。

- 修复 GM 平衡覆盖在旧生产目录迁移时可能丢失的问题：`balance_overrides.json` 与 `game.json` 独立迁移到 `shared/data`，删档只重置游戏存档；已有活动覆盖时不被旧副本覆盖，且修正兵种覆盖主键，使 GM 调整跨删档与部署持续生效。

- 修复幸福村被战斗清空后仍残留 PvE 实体、导致前往中的商队继续驶向已不存在目标的问题：清空不可重生 NPC 后立即移除目标并触发统一的目标消失返程；手动撤回商队按当前逐格行军进度计算返程 ETA，不再重置为整段去程；S4 释放路线点击“领取奖励”时，返回的奖励明细明确包含 500 金币与宝物。

- 修复 GM 任务模块编辑器的标签按钮模板引号错误；脚本现在可以正常启动、请求六张任务模块表并渲染编辑表格。

- GM 任务关系图对娜塔莉任务明确标注阶段 1 的清营目标与阶段 2 的处置选择；运行时面板同步显示当前阶段，避免把后续选择误当成同一目标的即时完成。

- 修复任务链 S3/S4：接取 S3 后立即在地图生成私有「幸福村」，贸易中心仅决定送达订单是否可接；掠夺幸福村获得的「秘密字条」始终进入报告等待确认；目的地营地或村庄消失时，攻击、掠夺、运输与商队均从当前位置立即返程。S4 清理营地后进入「处置被囚禁的娜塔莉们」阶段：释放才可手动领取任务奖励，放入宝库则保留该宝物并使任务失败。

- 修复地图缩放与军队点击交互：恢复左下角固定缩放控件（±/百分比，仅缩放 SVG 地图层）；点击格子时恢复选中底层格并弹出右侧交互面板——同格有他国军队显示脱敏军情，有己方驻扎军显示召回/行军，不再误走拓荒空地流程；补强 selected/foreignMoves 信号订阅以确保面板即时显示。

- 修复大地图悬停提示与敌方驻军识别：提示框改锚定六边形格心（不再跟鼠标漂移）；同格存在视野内他国军队时，悬停与点击优先展示「敌方军队」脱敏信息，不再误显示为「空地（可拓荒）」。

- 修复刚出征立即撤回时返程路径仅一格导致 `step()` 写入 `pos=undefined` 并卡死在途的问题。① 每次打开地图默认居中主城（不再记忆上次拖拽位置）；② 驻扎中（paused）军队不再绘制行军路线和行军标记（消除幽灵虚线）；③ 有部队驻扎的格子点击交互统一为选中底层格子（敌方军队信息仅在悬停时显示）。

- 修复大地图综合交互与渲染：`refreshAll` 误把 `mapCenter` 信号当坐标导致全图快照中心错乱；地形纹理 CSS 误设 `opacity:0` 导致贴图不可见；敌方军队行军线补绘（环面展开 + 半透明样式）；外国军队标记改按 `movementId` 定位避免重排错位；触摸点击与合成 click 去重；驻扎续行时查看敌方军队不再误取消续行；手机目标面板避开底部 Tab 栏；`refreshAll` 同步刷新外国军队。

- 修复大地图悬停/点击交互：提示框改跟鼠标位置、己方行军标记不再遮挡点击、敌方驻军悬停用 elementFromPoint 检测避免动画闪退；移除方向键与缩放比例 UI，缩放范围改为 80%–120%。

- 修复大地图四项交互问题：① 左下角「坐标跳转」面板提升层级并修正输入框被视角同步覆盖的问题；② 行军折线与部队标记按环面地图对齐当前视口副本，拖拽/跳转后路线不再消失；③ 格子悬停提示改为锚定六边形屏幕中心，不再与鼠标位置脱节；④ 缩放统一限制在 70%–220%，滚轮/双指到边界后不再无限缩放，并新增 ± 按钮与百分比显示。

- 修复新账号注册失败（操作失败）：建村坐标分配 `allocateSpot` 原先只把玩家主城当作“已占用”，漏算了世界里的 PvE / 任务营地 / 临时 PvE / 资源点等 `kind !== 'empty'` 地块；随机抽到被占格后 `world.PlaceVillage` 以 `tile_occupied` 拒绝，导致注册直接失败。现改为复用 `world.getOccupiedTileKeys()` 这一份与世界占用完全一致的真相：选到“完全空格子”（含 PvE 等临时内容皆不可）才落点，选到非空格子就随机换一个继续，**绝不报错放弃**；并给客户端 `village_creation_failed` 补充中文文案，避免将来再被通用「操作失败」吞掉。

- 修复「目标消失·原路返回」兜底：除 `pve.Remove` 外，直接在 `world.RemoveTile` 移除 PvE/任务营地地块时也补发 `pve.TargetRemoved`，确保无论地块由哪条路径移除，前往该目标的商队/出征都立即从当前位置返程，不会继续冲向已消失的目标后由 `arriveCaravan` 重算整段倒计时。

- 修复客户端误导文案：释放「被囚禁的娜塔莉们」（`captured_natalies`）的 toast 原谎称「释放即获得 500 金币与宝物「正直的心」」。服务端释放分支（`treasure.ClaimPending` release 分支）本就不发奖，奖励仅在任务栏点击「领取奖励」（`task.Deliver`）后由 `tasks.completeQuest` 发放；现 toast 改为如实说明「释放仅使娜塔莉获释，奖励需到任务栏领取」。

### 新增

- 扩展支线任务「调查坐标」：接取后于玩家村庄附近随机空地生成 3 个与老鼠窝同驻兵/掠夺资源的任务营地（rats 模板，真实占格、清空后不触发普通掉落）。清剿第 3 处营地时掉落宝物「被囚禁的娜塔莉们」（`captured_natalies`，popGrowth +20%，passive，走标准待领取报告流程，需军队归村后处理）。玩家处理该宝物二选一：① 放入宝库 → 获得 +20% 人口增长（无额外奖励）；② 释放（客户端以「释放」替代「丢弃」字样，并隐藏「出售」）→ 获得任务奖励 500 金币与宝物「正直的心」（`honest_heart`，epic/special）。新增复合效果类型 `honestHeart`（已注册进 `TREASURE_EFFECTS` 白名单）：全军攻击 +10%、全军防御 +10%、金币收入 +10%、科技点判定间隔 -10%（经 `research.SetTreasureTechInterval` 接入判定调度，倍率变化即按新间隔重调度 RP tick）；`ResearchState` 新增可选字段 `treasureTechIntervalMult`（默认 1，向后兼容）。网关 `ClaimPendingTreasure` 决策枚举新增 `release`；客户端待领取卡片为 `captured_natalies` 显示「释放」按钮、隐藏「出售」，`app/config.ts` 的 `treasureEffectText` 已补充复合效果文案。

- 新增支线任务「村民的请求」：成功掠夺（清空）普通 PvE 营地后，按 GM 概率触发；接取后玩家村庄 3 格内随机空地出现 NPC 村庄「幸福村」（pve 类型、0 守军、无重生）。玩家在贸易中心接取幸福村的粮食送达订单（500 粮食）并送达即完成任务，奖励宝物「娜塔莉」（人口增速 +10%，被动）；幸福村随后从地图消失。失败路径：若玩家选择掠夺幸福村（而非送达），任务失败并获得宝物「秘密字条」（instant）；使用后生成玩家村 5 格内 3 个随机空地坐标的战报，经 `treasure.ReportCoords` 推送并解锁后续支线「调查坐标」。

- 新增宝物「娜塔莉」（`natalie`，popGrowth +10%，passive，rare）与「秘密字条」（`secret_note`，reportCoords，instant，common），并补齐 `TREASURE_EFFECTS` 白名单的 `reportCoords` 分支（客户端 `TreasureReport` 通知已接入网关推送与通知映射）。

- 村民请求的全部可调参数（触发概率、订单资源种类与数量、幸福村兵力资源、战报数量与半径）均接入 `game_constants.csv`，运行时经 `config.constants.raw['key']` 读取。

- 视野内外国军队对外可见：地图打开时客户端每 3.5 秒轮询 `ListForeign`，服务端仅向对目标格拥有视野的玩家下发他国军队的脱敏视图（归属玩家名、归属城镇名、行军类型、坐标/路径/预计到达），不含兵力、携带物、战利品与宝物；含商队。点击该军队弹出只读信息卡。

- 修复探索行军路径虽已下发但缺少 CSS 绘制样式的问题：现在显示独立的蓝色流动虚线路线和望远镜部队点。

- 修复战争迷雾落地链路：地图为已探索格绘制灰雾且不在未探索格泄露任务营地；GM 兵种表可编辑视野，保存后的覆盖值会进入服务端运行配置。

- 修复 Windows 上提交闸门无法解析 `npm.cmd`、在验证前中断的问题。

- 修复 Windows 无创建符号链接权限时，Linux 发布目录布局测试把环境限制误报为代码失败的问题；正式部署仍保留 Linux 端验收。

- 修复地图会为超过集结点探索深度的未探索格显示“派军探索”入口；现在会隐藏入口并说明当前深度限制。

- 新增「目标消失即原路返回」：在途的进攻(attack)、掠夺(raid)、运输(transport)、商队(caravan)若目的地村庄被放弃/消失（触发 `world.VillageRemoved`）或 PvE 目标/NPC 幸福村被移除（触发 `pve.TargetRemoved`），会立即原地转为返程，从当前所在格沿原路返回出发村；运输/商队的货物与兵力一并退回发货村，商队到家后正常释放贸易路线。

- 修复行军视野已写入服务器但客户端地图未实时刷新迷雾的问题：军队每步完成视野更新后会向所属玩家推送刷新事件。

- 修正视野与驻扎/探索行军的存档契约：两者均以可选字段、缺省值和惰性创建兼容旧存档，不需要刷档；`SAVE_SCHEMA_VERSION` 保持 v2。

- 新增野外驻扎行军：可向任意地图坐标派军驻扎，目标到达前被设施或军队占据时自动停在前一格；驻扎军可召回或继续驻扎、掠夺、攻城。集结点等级决定行军点上限，每支离城军持续占用一个行军点。

- 新增服务器权威的战争迷雾：城池与在途军队共同提供视野；地块区分未探索、已探索和当前可见，已探索仅保留最后一次观测快照。兵种增加可配置视野属性，军队按数量最多的兵种（并列取视野最大）决定视野。

- 移除任务奖励日上限和酒馆等级奖励加成；贸易中心宝物栏满时替换会按被替换宝物的售价自动出售，并在界面明确显示该售价。

- 重构宝物栏：取消类别生效槽，城镇中心与宝库内的全部宝物（含重复）同时生效；任务奖励满栏改入待处理报告，营地待领取可在有贸易中心时出售；宝库缩容按新容量保留高价值宝物，其余进入待处理报告。

- 「耀武扬威」改为合格军旗归城后手动交付：仅交付仍存于城镇中心或宝库的合格军旗，并随机选择一面；胜利的旗帜宝物卡补充效果说明。

- GM 任务管理支持把已完成支线标记为未完成；有触发条件的任务须重新触发后才可再次接取。

- 修复「耀武扬威」清理 PvE 营地未结算：战斗结束事件现透传营地已清空状态。

- 修复「耀武扬威」仅在新训练士兵时触发的问题：服务器恢复时也会检查已达 20 人兵力的村庄。

- 新增支线任务「耀武扬威」：兵力达到 20 后可接取，携带军旗和至少 20 名士兵清理任意营地并归城，自动以军旗兑换「胜利的旗帜」。
- 新增史诗宝物「胜利的旗帜」（不参与掉落）：基础攻防 +10%；携带它成功清营或成功掠夺并归城后额外 +2%，易主后额外加成重置。

### 变更

- 开发 worktree 增加独立依赖准备命令；提交钩子改为先完成瞬时提交信息校验，再运行完整验证。生产发布默认使用专用回连 SSH key，并在耗时验证前检查部署通道，避免认证失败或格式错误重复消耗完整测试。

- 清理营地任务卡新增未清理营地的 X/Y 坐标；点击坐标或“前往地图”会定位并高亮对应任务营地。

- 生产部署改为不可变 `releases/<SHA>` + `current` 原子切换，存档与日志迁入 `shared/`；生产目录不再作为 Git 工作树，并补齐多人协作 Git 默认配置、作者身份守卫与 PR 模板。

- 地图出征编队新增单兵种与全军 `MAX` 快捷按钮；人口资源条在红框悬浮说明中明确展示饥荒或仓储溢出造成的人口增长扣减。

### 修复

- 修复旧存档中通过历史锁定桶保存的任务宝物（如“勇士之证”）不显示在宝库/城镇中心、无法交互的问题；现在会自动迁入正常宝物栏，满栏时转为报告中的待处理宝物。

- 地图实体名称改为固定渲染在所属六边形下半部内侧，不再落在相邻格上而被遮挡或显得漂浮。

- 任务营地生成时写入所属村庄；启动恢复会为历史营地补齐 owner 并将错误遗留的全局 `pve` 地块收回私有 `taskcamp`，其他玩家不再可见或互动。

- GM 平衡覆盖在启动、热重载与刷档后保持权威；补齐热重载时新玩家出生地图尺寸与分城放弃保护时间的配置更新。人口快照现下发真实增长速率而非住房剩余缺口，祭祀台扣人口后不会再错误显示为极低增长率。

- 野战相遇后，胜方阵亡部队立即从在途人口足迹移除，不再把死亡兵力错误保留到后续到达或返程。

- 清理任务营地后不再残留地图任务标；服务端与客户端均过滤已清理营地。战斗中的出征部队持续计入人口足迹，避免幸存者返程时村庄人口被重复结算而凭空增加。
- 发布切换时重建 PM2 进程，确保历史 `src/main.ts` 进程真正切到 `current` 的生产构建并上报正确 release SHA；发布包不再携带 macOS 扩展属性。
- 出征（行军）部队仍计口粮：在途兵力单独计入 upkeep，不再「派出去就不耗粮」。
- 任务营地在地图上正确显示为可攻打据点（不再误显示为荒地/拓荒），可直接派兵攻打。
- 上交资源输入栏设上限并移除上下箭头：无法再输入大于剩余需求的数字。

- 修复商队目标村庄/营地消失后虽已在服务端转返程，但缺少 `movement.Sent` 广播导致客户端 `ListForeign` 缓存不刷新、视觉上仍显示"未返程/运送倒计时被重置"的问题：现在 `startReturn` 末尾补发 `movement.Sent`，客户端重拉行军列表即正确显示返程。

- 修复支线任务「调查坐标」过早出现"领取任务奖励"入口的问题：现改为玩家清剿营地获得「被囚禁的娜塔莉们」后，任务进入**等待抉择**状态（ready=false），不提供领取入口；待玩家在待处理报告中对该宝物做出选择（放入宝库→`natalieDecision='store'`，或释放→`natalieDecision='release'`）后任务才进入就绪，按钮文案随之变为「完成任务」或「领取奖励」（释放奖励为「正直的心」）。超时未处理会重新发放该宝物给玩家再次抉择。

- 修复「胜利的旗帜」在掠夺完成后放入**报告**（待处理）而非宝库时未获得 +2% 额外加成的问题：现在 `storeCarried`/`claimPending` 在入库或报告中收下胜利旗帜且携带资格（`victoryFlagQualified`）成立时，均正确累加 `victoryFlagBonus += 2`；资格标记在结算后清理。

- 修复商队（含贸易商队/运输）在目标村庄被放弃或遭删村（`wipeSingleVillage` 真实路径）后不返程、地图仍显示原行程倒计时的问题：清村时现在会广播 `world.VillageRemoved` 并保留来向该村的在途行军，交由 `movement.onVillageRemoved→startReturn` 就地改写为返程。此前仅 `world.ClearVillage` 测试命令会广播该事件，正常放弃/删村路径静默删除行军记录，导致 `onVillageRemoved` 永不触发、客户端倒计时不刷新。

- 修复释放「被囚禁的娜塔莉们」时立即发奖、与"需点领取奖励后才获得"相悖的问题：500 金币与宝物「正直的心」的发放从 `treasure.ClaimPending` 的 release 分支移至 `task.completeQuest`，仅在玩家点「领取奖励」（`task.Deliver`）且 `natalieDecision==='release'` 时发放。

### 新增

- GM 任务管理新增「重新触发放弃任务」按钮：已放弃（`abandonedSide`）的支线可一键移出放弃列表、补回触发条件、清除冷却并重新解锁，使其可再次接取；新增 `task.GmRetriggerAbandoned` 命令与对应 GM 面板入口。

- 地图新增始终可见的战术定位台：支持输入 X/Y 坐标跳转，并可一键将视角锁定回自己的主城；手机端同样保留完整定位能力。

- 任务改为「达成条件后需手动交付领取奖励」：目标达成后任务进入**就绪可交付**状态（不自动发奖、不解锁下游），玩家点「交付领取奖励」才发放奖励并弹窗展示获得的资源/宝物。新增 `task.Deliver` 命令；上交资源类任务仍保持「不能超额上交、输入上限随剩余需求变动」。
- 浏览器登录态改为可自动续期的 30 天安全会话，WebSocket 空闲、网络切换、电脑休眠或服务端部署重启后会自动恢复账号与当前村庄；服务端增加 WebSocket ping/pong 保活，正常阅读页面不再因 10 分钟无操作退出。
- 前端构建增加唯一版本指纹与 `/version` 无缓存探针；服务器部署新代码后，已打开页面会在连接恢复、切回标签页或最多 1 分钟内更新 Service Worker 并自动刷新一次，无需手动清缓存或刷新页面。
- 顶栏新增「退出登录/切换账号」按钮：清除会话 token 回到登录页，可重新登录或注册新账号。

- [需刷档] 科研改为目录表 + `research_effects.csv` 多效果明细，修正效果覆盖、无效描述、跨村继承与学院拆除清空问题；PvP 新增安全库存、连续受击衰减、强弱差战利品曲线和战败恢复底线；雇佣兵新增统御容量与限时合同；宝物新增分类生效槽、同名唯一生效及加算封顶；随机任务新增重复冷却、每日奖励预算和拥挤地图营地重试。相关阈值均迁入 CSV / `game_constants.csv`。

### 变更

- 生产部署与本地提交解耦：`git commit` 只执行本地完整验证；`npm run deploy:prod` 可从任意本地分支发起，但仅验证和发布远程 `origin/main` 的确定提交，公网 `/version` 核对 release SHA，不一致自动回滚。
- 接入路由从领域模块移到 `gateway/routes.ts`，领域层不再反向依赖 gateway；同时移除误暴露到公网的行军/宝物内部命令，补充分层守卫。
- 精简项目知识入口：`PROJECT.md` 收敛为当前架构/owner/文档路由，8 篇重复索引或已实现设计进入 archive；当前架构文档删除过期的“Combat 无状态”等历史描述。
- 应用组装统一 init/resume/reload 生命周期清单，配置覆盖逻辑从巨型 `infra/config.ts` 分离到 `balance-overrides.ts`；内部消息 handler 默认类型由 `any` 收紧为对象类型；服务端测试在完成 TAP 输出后强制退出，避免残留调度句柄卡住验证流水线。
- 村庄页改为宽版双栏城务台账：左侧约三分之二按“城池详情 → 指挥台 → 人口 → 宝物”纵向排列，城池全景恢复真实 3:2 比例并占满列宽；右侧约三分之一常驻建筑清单、建造队列与任务管理，取消会隐藏信息的全景/清单视图切换。
- 重排村庄驾驶舱的决策层级：桌面端保留“领地全景 + 右侧决策台”，手机端改为“领地全景 → 紧凑决策台 → 深层数据”，并统一中文界面语言、移除视图切换中的 emoji。
- 重构客户端视觉为现代大战略战术 HUD：启用全新主城场景、去除地图 Travian 式重复贴图表现，并统一页面底色、字体层级、建筑悬停与低遮挡地图交互。
- 伯乐「翻倍骑兵」取消 5% 安全边距（原 `ratio×0.95`），改为按资源/劳动人口全额翻倍。

### 破坏性

- Wire 协议升级到 v3（最低兼容 v2）：公网 action 仅保留真实客户端能力，移除误暴露的行军查询与宝物内部协作命令；旧页面收到版本不匹配后会通过版本探针刷新。

### 新增

- 新增提交前部署总闸门：`git commit` 强制完整构建/静态检查/全部测试，隔离启动真实生产产物并验证 HTTP、前端静态资源、WebSocket 注册与主界面快照，再部署同一候选快照到腾讯云并从公网只读验收；失败自动恢复部署前代码并重载 PM2。CI 同步执行本地生产冒烟，正式存档始终排除在测试和回滚之外。
- 恢复批量被部署 reset 丢失的科技与配套功能（从 `_auth` 副本 git 历史重建）：**全民皆兵**（`universal_conscription`）+ **露天仓库**（`open_warehouse`，`storage_overflow` 机制，无该科技时 `economy.Grant` 超额资源被丢弃、有科技可溢出至 `capacity×(1+overflowCap)`，`productionPaused` 满仓即停产，客户端人口面板溢出警告）。同时恢复 `build_speed` 科技注入、`research` 重启重新 apply、科技/宝物效果白名单校验、`trade` 热重载重排刷新/宝物出现改 dropRate 权重、打野宝物未归村隐藏信息、客户端科技页无学院锁屏。同步更新相关测试对齐超额语义。

- 新增宝物「伯乐」（`bole`，`cavalryTrainSpeed`，id 19）：被动=骑兵训练时间减半（`military.treasureCavalryTrainMult`，`treasures.aggregate` 按 `1-effectValue/100` 累乘、`recomputeAndPush` **总是下发**避免移除后残留）；使用=按现有骑兵等比例翻倍（`military.DuplicateCavalry`：查资源/劳动人口算 95% 安全 ratio → `economy.TrySpend` 扣资源 → `population.ConvertPopToGarrison` 直接转劳动人口为驻军，不走训练预留通道），客户端「使用」按钮按 `effectType` 分派并展示翻倍明细 toast。此宝物曾在 8/11 以未提交改动被部署 reset 回滚丢失，本次从 `_auth` 副本 git 历史完整恢复并以新 id 重建。
- 任务系统新增「触发条件」：`quests.csv` 增加 `trigger` 列，随机任务可设 `building_built:<建筑code>`（建造完成该建筑后才进酒馆）；服务端 `tasks` 监听 `building.Built` 标记触发并立即 offer，`TaskState.firedTriggers` 持久化，GM 任务目录编辑器展示 trigger 列与说明。新增任务目标 `sell_discard_treasure`（累计出售/丢弃 N 个稀有及以上品质宝物）：`treasure.Sell/Discard` 广播 `treasure.SoldDiscarded`，`tasks` 计数推进并完成后发奖；客户端任务卡显示目标与进度。
- 新增宝物「祭祀台」（`ritual_altar`，`ritualBuff`）：使用后扣除 `ritual_buff_pop_cost` 个劳动人口（不足按 popCost 升序转扣士兵、允许超扣），获得全资源产量 +25% 持续 `ritual_buff_duration_sec`（默认 2 小时）。`economy` 新增定时 buff 层（`ApplyTimedBuff`，到期自动失效、重启自愈），`population.ConsumeLabor` / `military.SacrificeTroops` 支撑人口扣除；即时类任务奖励宝物改为不锁定（供玩家使用），GM 新增 `/gm/ops/sell-treasure`、`/gm/ops/discard-treasure`。
- 新增任务「献祭筹备」(`r4`)：触发条件=建造完成宝库，目标=出售/丢弃 2 个稀有+宝物，奖励=祭祀台。
- 修复 任务营地可见性泄露与越权攻击：任务营地改为写入独立地块类型 `taskcamp`（不再混入全局 `pve`），`world.getArea` 过滤该类型，仅任务拥有者经 `taskMarkers`(🎯) 可见，其他玩家地图与行军均看不到/误击；`findFreeTile` 天然跳过占用格避免营地冲突。新增 `pve` 任务营地 `ownerVillageId` 归属，`movement.SendRaid` 校验仅拥有者本村可攻击任务营地（越权返回 `not_task_owner`）。
- 新增 15 张科技节点战术徽记，补齐 `research.csv` 的全部美术引用；科技树不再退化为文字占位图标。
- 新增测试入口登记、推送映射一致性与 GM HTTP 路由闸口，避免测试静默漏跑、推送契约漂移及危险路由绕过鉴权。
- 补齐贸易资源守恒与路线生命周期、雇佣兵招募与刷新、科研完整研发与拆除边界的端到端覆盖。
- 新增任务系统（服务端 `tasks` 模块 + 客户端任务栏/地图营地标记）：主线/随机任务接取·放弃·上交资源推进，酒馆建造触发随机任务刷新，`clear_camp` 任务在地图生成营地、清掉后移除；接取/放弃/完成经 `TaskListChanged`/`TaskMapUpdated` 推送刷新。
- 任务模块接入 GM 面板：新增 `/gm/tasks` 实时任务管理（完成/放弃/接取/上交资源/刷新随机/重置本村）与 `/gm/quests` 可视化编辑器（编辑 `quests.csv` 任务目录并热重载生效），配套 GM 运维命令 `task.GmComplete`/`task.GmRefreshRandom`/`task.GmReset`；修复 `serializeCsv` 写回时丢失 UTF-8 BOM 与末尾多余空行的 fidelity 问题。
- 任务卡片显示奖励：服务端 tasks 模块在 serializeInstance 与酒馆 offered 序列化中补下 rewards（资源含金币 + 任务专属宝物 code），客户端任务卡与酒馆委托卡新增 RewardRow 渲染奖励（资源用 resInfo + CostRow，宝物用 treasureInfo 解析名称/图标 + treasureEffectText 悬停说明），并在 village.css 增加奖励区样式。


### 变更

- 前端美术与表现升级为“边境战争沙盘”方向：重构应用 HUD、村庄经营驾驶舱、地图出征工作流、科技节点路径与报告时间线，并统一提高字体对比度、触控尺寸和图片承载清晰度。
- `PROJECT.md` 测试索引改以 `all.test.ts` 为唯一清单，并压缩回行数预算；`README.md` 同步当前技术栈、功能与验证命令。
- 导出网关 `MODULE_MANIFESTS` 与通知 `EVENT_MAP` 供契约测试读取唯一真相，并移除从未发射的陈旧 `treasure.Dropped` 映射。

### 新增

- **前端视图层改为 Preact + @preact/signals**：组件化渲染取代模板字符串 `innerHTML` 拼接；`app/store.ts` 提供 1 秒心跳 `tick`、数据版本 `dataVersion`、弹层栈与 Toast，数据刷新只做局部重渲，不再整页重建 DOM（顺带消灭了「刷新打断输入/丢焦点」和地图拖拽被打断的老毛病，`isInteracting()` 兜底逻辑随之删除）
- **全新设计系统**（`src/styles/tokens.css` + `frame.css`）：深色石质面板 + 鎏金浮雕描边的视觉基调，配色/间距/字号/层级全部收敛为 CSS 变量；配套 UI 原子 `src/ui/`（`Icon`/`IconPlate`/`Panel`/`Btn`/`Bar`/`TimerBar`/`CostRow`/`Modal`/`Stat`）
- **村庄可视化场景**：主界面新增手绘俯视村庄底图，建筑按固定坐标坐在垫台上、点击弹面板，保留「列表管理视图」一键切换（偏好存 localStorage）
- **地图改用真实地形贴图**：9 种手绘地块（草原×3/森林/丘陵/水域/绿洲/村落/废墟）裁进六边形，村庄/野怪按归属与难度用描边色区分
- **实时战斗面板**：服务端本就在推 `BattleTick`，此前前端完全没展示；现在报告页顶部显示双方兵力对抗条、逐兵种残余与战损
- **铁匠铺升级入口**：服务端 `UpgradeSmithy` 动作此前无任何界面可用，现补上锻造面板 —— 精确造价（木/泥各 `smithyCostBase`×目标等级，与服务端同公式）、基础耗时、真实起止时刻的进度条、资源不足与互斥占用的禁用理由
- 服务端补齐锻造界面所需数据：`GetGameConfig.constants` 增发 `smithyCostBase`，`GetArmy` 增发 `pendingSmithy { unit, startAt, doneAt }`（调度器内部 `taskId` 不外泄）；`pendingSmithy.startAt` 为新增可选字段，老存档缺失时读取方按 `null` 兜底，**不需要刷档**
- `server/src/test/smithy.test.ts`：铁匠养成此前零测试覆盖，补上造价公式、`pendingSmithy` 下发契约、并发互斥、资源不足四项
- **战报结构化**：战报从裸字符串改为 `StoredReport { text, kind, ts }`，分类由 `notificationKind(event, payload)` 在写入时按**事件名**算好（不是回头正则匹配中文文案——宝物名里带「人口」就会误判）；报告页按类别上图标色带并支持筛选
- 手机端专属布局：紧凑顶栏 + 资源条横向滚动 + 贴底页签栏，弹窗改贴底抽屉，点击目标 ≥44px
- 美术流水线 `tools/art_pipeline.py`（洋红幕布抠像 + 去溢色 + 裁切 + 降采样 + WebP 编码）、资产清单 `tools/art_manifest.json`（123 条）、抠像自查 `tools/art_check.py`、拼版自查 `tools/art_sheet.py`、底图垫台测量 `tools/scene_pads.py`
- 新增 `docs/2_2.0设计/14_前端设计系统.md`：前端视图层契约（架构三条规矩 / 设计令牌 / UI 原子 / 跨特性接口 / 自查清单）
- 引入《变更契约》与四道自动闸门：`npm run guard` 校验文档分层/front-matter/索引一致/CHANGELOG 记账/版本号同步，挂到 `pre-commit`、`commit-msg`、`pre-push` 与 GitHub CI
- 新增存档结构版本号 `SAVE_SCHEMA_VERSION`（`packages/server/src/infra/schema-version.ts`），与 CHANGELOG 的刷档声明双向绑定

### 变更

- 科研系统接入新前端：科技页（科研点面板 + 三分支 + 分层卡片）与学院弹窗按新设计系统重写，新增「科技」页签，`RpChanged`/`TechCompleted` 推送只重拉科研快照
- 补齐并行合入的科研系统欠下的变更契约债：`research` 模块与 `15_科研系统设计.md` 进 PROJECT.md 索引、`research.csv`/`academy.csv` 进 `config/README.md`、三篇文档补 front-matter
- `PROJECT.md` 超出 300 行预算（合并后 516 行），曾按 R1 把「项目约定与坑 / 速查表」拆到 `docs/archive/项目约定与速查.md`，金币小节收敛为要点表并指向 `docs/经济与金币模块.md`，目录树不再重复罗列 CSV（`config/README.md` 才是唯一索引）
- **全套美术按新风格重做并补齐**：123 张资源（原 45 张，其中 5 张还是占位图；配置里被引用却缺文件的 52 个基名全部补上——高卢/条顿各 10 兵种、10 雇佣兵、16 宝物、5 建筑、金币图标）
- **美术格式从 PNG 改为 WebP**：同画质体积降到约 1/5，全套约 5MB；`artPath()` 统一拼 `.webp`，仅 `ui_logo` 另存 PNG 供 PWA 图标使用
- 建筑/兵种/宝物图标统一坐在凹陷石板底座上（`IconPlate`），透明美术不再悬浮参差；图标显示尺寸整体放大，美术终于看得清
- 危险操作（拆除/解散/放弃村庄）从 `window.confirm` 改为面板内二次确认
- 每个页面都有真正的空态引导（军队页与报告页此前是整片空白）
- 战报文案去掉 emoji 前缀，改由界面的分类图标承担语义
- `CLAUDE.md` 收敛为 L0 入口（红线 + 任务路由表），`PROJECT.md` 成为模块清单与文档清单的唯一索引；旧二级索引已归档到 `docs/archive/00_README.md` 与 `docs/archive/服务器_README.md`
- 已上线系统的一次性实现规划（建筑重构、人口 v2）归档到 `docs/archive/`；`美术生成-成品prompt-中文.md` 被 `tools/art_manifest.json` 取代后一并归档
- Service Worker 缓存名升到 `kow-v2`，PWA 主题色改为深色，强制作废旧壳与旧图

### 修复

- 修复 session token 伪造测试偶发未改变 token 最后一位而误判成功的问题，使完整服务端测试稳定退出并返回真实状态码。
- 修复叠加二级弹窗被顶部导航遮住、动态视口下标题区不可见；统一村庄建筑等级牌到建筑落脚线并收紧手机端建筑热区，避免重叠误点；取消“当前决策”侧栏粘滞滚动，并将建筑拆除、学院拆除与部队解散收纳进折叠的次级管理入口，避免危险操作挤占主决策区；同时为丢弃宝物、遗弃待领取宝物和放弃任务补齐不可逆操作确认。

- **所有耗时/倒计时显示恒为「0秒」**：时长格式化只有一个收「目标时刻」的函数，而消耗预览、建造进度条等调用方传的是**时长**，`ms − Date.now()` 变成大负数被夹到 0。拆成 `fmtDur`（收时长）与 `secLeft`（收时刻）两个语义明确的原语，补上回归测试，并顺手支持「时」量级（长建造不再显示「180分0秒」）
- `BattleTick` 覆盖式写入战斗快照，会把只在 `BattleStarted` 里出现的攻防数值与目标信息抹掉；改为合并写入
- 历史战报的时间戳取错字段（`notification.at`，实际是 `ts`），导致登录后拉回的战报时间全是 Invalid Date
- `PopulationChanged` 推送是增量载荷，此前按全量字段套用会把上限、繁荣度等一片数值误清成 0；现在缺字段一律沿用旧快照
- 地图拖拽、双指移动或缩放导致主城偏离中心后，方向键中央的回城键会立即启用并可一键重新居中

### 移除

- 旧的字符串渲染层：`app/bootstrap.ts`、`style.css`（965 行单文件）、`shared/ui/widgets.ts`、`shared/ui/toast.ts` 及各 feature 的 `render*/bind*` 实现
- 过时的美术脚本 `tools/gen_art.py`（128px 占位图生成器）与 `tools/optimize_art.py`（PNG 降采样），职责已并入 `tools/art_pipeline.py`

---

## 更早

0.0.1 之前没有正式发版记录，历史请查 `git log`。从本版本起所有改动都在上面记账。
