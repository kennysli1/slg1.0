# 13 · 人口系统设计（繁荣度驱动 · 粮食软上限 · 文明活力）

> 状态：**设计定稿（2026-07），待实现**。
> 本文是「人口系统」的唯一设计入口，涵盖机制 A–H、完整数值表、收敛演算与模块边界。
> 数值均基于 `config/buildings.csv`（prodBase=1000, prodGrowth=1.3, timeBase=1.5s, timeGrowth=1.6）、
> `config/units.csv`（30 种兵，三部族）、`config/game_constants.csv` 现有参数推导，不凭空捏造量级。
> **2026-07 修订**：将隐式作用统一到"速率 vs 存量"判断框架（见下）；
> 建筑级别的劳动力饱和参数从全局常量下沉至 `buildings.csv` 三列（`laborAmplified / laborSaturation / laborBonusMax`）；
> 删除防守加成效应（城墙/驻军决定的静态防御不受人口影响）。

---

## 一、设计目标

**一句话定位**：人口是城市的「文明活力」——它不仅是兵力池，更是驱动经济、研究、练兵、锻造和建设的综合生命力指标。损失人口会同时拖慢**资源产出/研究/练兵/锻造/建造**五条速率轴，从而给战争行为真实定价。

**两条驱动轴**：

| 轴 | 机制 | 直觉 |
|----|------|------|
| **繁荣度轴** | 建筑越多、等级越高 → 繁荣度越高 → 人口增长越快 | "城市越发达，人越多" |
| **粮食轴** | 粮食净产量决定「软上限」 → 超过软上限时人口开始减少 | "养得起多少人由粮食说了算" |

两轴分工：繁荣度管「增得多快」，粮食管「最多活多少」。互相独立，各施影响。

**判断标准（核心原则）**：
> 人口 = 人手/劳动力。它加速的是"靠人做的、有速率的过程"（速率类），
> 改变不了"靠材料/等级堆出来的静态属性"（存量/物理类）。

| 速率类（吃人口） | 存量/物理类（不吃人口） |
|----------------|----------------------|
| 资源产出速率、研究速率、练兵速率、锻造速率、建造速度 | 仓库/粮仓容量、城墙防御值/耐久、集结点集结容量 |

**设计原则**：
- 人口用**隐式影响**（自动调节）而非显式劳动力分配拨杆，与 SLG 异步放置节奏匹配。
- UI 明确展示人口数、软上限、各项速率加成百分比，让玩家理解系统而不会困惑。
- 两类隐式作用：**劳动力增幅**（五类速率）+ **伤兵治疗加速**，共同构成「人口=文明活力」整体感知。

---

## 二、核心机制（A–H）

### A. 繁荣度（Prosperity）

```
繁荣度 = Σ( 建筑等级 × prosperityPerLevel )   ，对城内所有已建建筑求和
```

- `prosperityPerLevel` 是每个建筑每级贡献的繁荣度，**所有建筑统一取 5**（不区分军事/经济，理由：军人也是就业，兵营也繁荣城市）。
- 繁荣度是**只读派生值**，由 building 模块在建筑完工/升级时计算并通过 Event 通知。
- **繁荣度只决定人口增长速率，不决定上限**。

> 例：7 栋建筑各 Lv1（新村） → 繁荣度 = 7×1×5 = 35
> 10 栋建筑均 Lv8（中期） → 繁荣度 = 10×8×5 = 400

---

### B. 人口增长与粮食软上限

#### B-1. 人口增长

```
增长速率 (pop/hr) = 繁荣度 × pop_growth_per_prosperity
```

- 人口按此速率**持续增长**（与 economy 同款惰性结算，读写时按时间差补算）。
- **没有硬上限**；增长只被粮食的软上限间接制约（见 B-2）。
- **新村开局满员**：新村创建时 `currentPop` 直接设为当前软上限值，无需等待增长。

#### B-2. 粮食软上限

```
软上限 L = ( 粮食产量 × 劳动加成(P) − 建筑维护 − 军队维护 ) ÷ pop_per_capita_crop
```

- **粮食产量**：含劳动力加成后的实际产率（见 H-①，农田适用速率类劳动力模型）。
- **建筑维护**：现有 `sumPop(lv)=lv×(lv+1)/2` 机制（不变，沿用现有 economy 计算）。
- **军队维护**：`Σ(unit.upkeep)`（现有机制，不变）。
- `pop_per_capita_crop`：每人每小时消耗的粮食量（新增常量）。

#### B-3. 人口与软上限的动态关系

| 当前状态 | 行为 |
|----------|------|
| `currentPop < L`（低于软上限） | 人口以 `繁荣度 × 增长系数` 速率持续增长；粮仓有净余流入 |
| `currentPop ≈ L`（接近软上限） | 人口增长被粮食消耗平衡，趋向稳定 |
| `currentPop > L`（超过软上限） | 净粮为负；粮仓开始消耗；**粮仓耗尽后**人口开始减少，净粮赤字越大减得越快 |

> 粮仓作为缓冲：超过软上限不立刻掉人口，而是先耗粮仓储，为玩家留下反应时间（建新农田/解散部分军队）。

---

### C. 造兵消耗人口

- 训练开始时**一次性扣除** `数量 × 兵种.popCost` 的人口。
- 若 `currentPop < 所需消耗`，训练被拒绝，返回错误 `insufficient_population`。
- 被扣除的人口进入「士兵形态」——活着的士兵不归还人口，主动解散才归还（见 E）。
- 军队战死则走伤兵机制（见 D），不是直接归还人口。

---

### D. 伤兵机制（战斗死亡 → 部分恢复为人口）

**触发条件**：战斗结束，某方损失 N 名士兵时。

```
伤兵数 = floor( N × unit.popCost × pop_death_recovery_ratio )
永久阵亡的人口 = N × unit.popCost − 伤兵数
```

**流程**：
1. `伤兵数` 个人口**从士兵状态移入伤兵队列**（entry: `{count, healAt: now + healTime}`）。
2. `healAt` 到达时，`currentPop += count`（人回到城市，但不自动变成士兵，需玩家重新花资源训练）。
3. `永久阵亡的人口` 彻底消失，不进入任何池。

**伤兵池独立**：`woundedPool` 是独立队列，不计入 `currentPop`，也不占粮食软上限（不吃粮）。治愈后才加回 `currentPop`。

**双方各结算**：攻守双方各自向自己的城市发 `population.AddWounded` Command，伤兵回各自城市医疗。

**扩展钩子**：`pop_death_recovery_ratio` 和 `pop_heal_time` 均为全局常量；未来「医院/伤兵营」建筑可通过修改器提升恢复比例或缩短治疗时间（population 模块的派生管线加一层，不改主流程）。

---

### E. 主动解散军队

- 驻守在城内的军队可以主动解散：`currentPop += 解散数量 × unit.popCost`，100% 归还人口；**资源不返还**。
- **出征中的军队不能解散**（在途部队由 movement 模块管辖，不在城内）。

---

### F. 特殊单位的人口处理

| 单位类型 | 人口规则 |
|---------|---------|
| **拓荒者** | 训练消耗 `popCost=5`；派出后**永久消耗**，人口不返还（人随拓荒者去建新村了）。解散无效（出征中不能解散） |
| **首领/元老**（senator/chief） | 与普通士兵相同：战斗死亡走伤兵机制，主动解散归还人口 |

---

### G. 建筑被摧毁后的人口响应

攻城锤/投石机破坏建筑 → 繁荣度下降 + 粮食产量下降：

| 效果链 | 说明 |
|--------|------|
| 繁荣度下降 | 增长速率降低；人口**不被硬砍**，只是增长放缓 |
| 粮食产量下降 | 软上限下降；若当前人口超过新软上限，粮仓开始消耗，最终人口缓慢向新软上限回落 |
| 回落速率 | 由净粮赤字决定（非瞬间崩塌），攻击方破坏建筑的人口代价体现为"城市恢复周期" |

---

### H. 人口的两类隐式作用（文明活力）

人口通过以下两大类路径自动影响全局，无需手动分配劳动力。UI 应明确展示各项加成数字。

---

#### H-① 劳动力增幅（统一速率模型）

**核心原则**：人口作为劳动力，加速"靠人做的、有速率的过程"。每栋 `laborAmplified=1` 的建筑独立地用"全城总人口 vs 该建筑自身饱和阈值"算出劳动比，再套入速率公式。一座人口兴旺的城市同时加速上述所有速率过程。

##### 劳动力饱和阈值

对于等级 L 的建筑 b，其饱和阈值 `S_b(L)`：

```
资源田（prodBase 有值）：  S_b(L) = laborSaturation_b × prodGrowth^(L−1)
其他建筑（academy/smithy/barracks/stable/workshop/main）：
                           S_b(L) = laborSaturation_b × L
```

> 资源田用指数曲线（与产量曲线同步），其余建筑用线性曲线（简洁，直觉合理）。

##### 速率类建筑（资源田、学院、兵营、马厩、兵工厂、铁匠铺）

劳动比：`r_b(L) = min(1.0, P / S_b(L))`

实际速率倍率：
```
rate_mult_b = pop_labor_floor + laborBonusMax_b × r_b(L)
            = 0.75 + laborBonusMax_b × min(1.0, P / S_b(L))
```

实际速率（产率/研究速率/练兵速率/锻造速率）：
```
实际速率 = 基础速率 × rate_mult_b
实际耗时 = 基础耗时 ÷ rate_mult_b
```

| 建筑 | 影响的"速率" | 零人口时 | 满员饱和时 |
|------|------------|---------|----------|
| 农田/伐木/采泥/铁矿 | 对应资源产出速率 | 基础×0.75（75%产率） | 基础×1.00（满产率） |
| 学院 | 研究速率 | 基础×0.75（研究慢33%） | 基础×1.00 |
| 兵营/马厩/兵工厂 | 对应兵种训练速率（trainSec 缩短） | 基础×0.75（练兵慢33%） | 基础×1.00（基础速） |
| 铁匠铺 | 铁匠升级速率（每次升级时长） | 基础×0.75 | 基础×1.00 |

> 练兵加速与"练兵消耗人口"自洽：人多→兵源足、征得快。
>
> `pop_labor_floor = 0.75` 为全局下限常量，对所有速率类建筑统一适用，防止死亡螺旋。

##### 建造速度（城镇中心）

城镇中心的劳动比：`r_main(L) = min(1.0, P / (laborSaturation_main × L))` = `min(1.0, P / (400 × L))`。

建造时间倍率（"减少时间"与"提升速率"方向相反，故单独公式）：
```
time_mult_pop = 1 − laborBonusMax_main × r_main(L)
              = 1 − 0.20 × min(1.0, P / (400 × L))
```

最终建造时间（与主基地等级加速叠乘，两层来自不同 modifier，各自独立）：
```
实际建造时间 = 基础时间 × main等级加速因子 × time_mult_pop
```

| 人口状态 | time_mult_pop | 等效加速（相对基础时间） |
|---------|---------------|---------------------|
| 满员（P ≥ S_main） | **0.80** | 节省 20% |
| 半员（P = 0.5×S_main） | **0.90** | 节省 10% |
| 零人口 | **1.00** | 无人口加速（主基地等级加速仍有效） |

> 主基地等级加速（最大-60%）与人口加速（最大-20%）叠乘：满级主基地 + 满员时，
> 基础时间 × 0.40 × 0.80 = 基础时间 × 0.32（总节省 68%）。
>
> 槽位数量（可同时建造几条队列）由主基地等级派生，**不受人口影响**（存量，非速率）。

---

#### H-② 加速伤兵治疗

治疗是速率过程（每批伤兵经历一段恢复时长），符合"劳动力加速速率"原则；但治疗加速不依赖单个建筑，而是用全城人口充裕度（λ）驱动。

定义**充裕比** `λ = min(1.0, currentPop / max(1, soft_limit))`（当前人口与软上限之比）。

```
实际治疗时长 = pop_heal_time / ( 1 + pop_heal_bonus × λ )
             = 3600 / ( 1 + 0.50 × λ )
```

| λ | 治疗时长 | 备注 |
|---|---------|------|
| 1.0（满员） | 3600/1.5 = **2400s（40分钟）** | 人多治疗快 |
| 0.5（半员） | 3600/1.25 = **2880s（48分钟）** | |
| 0（零人口） | 3600/1.0 = **3600s（60分钟）** | 人少治疗慢 |

由 population 模块内部调度器处理伤兵治愈时机（Scheduler 驱动，不用 setInterval）。

---

#### 两类作用的协同感知

两条路径共同构成「人口 = 文明活力」的整体体验：

```
人口下降
  ├→ 五类速率劳动力不足
  │    ├→ 资源产率下降（最多−25%）→ 经济受损、软上限下移
  │    ├→ 研究速率下降（最多−25%）→ 科技进度延迟
  │    ├→ 练兵速率下降（最多−25%）→ 补兵更慢
  │    ├→ 锻造速率下降（最多−25%）→ 装备升级更慢
  │    └→ 建造加速消失（最多−20%）→ 升级建筑更慢 → 恢复更慢
  └→ 伤兵治疗减速       → 伤兵回血更慢       → 恢复期更长
```

战争造成士兵死亡 → 人口永久流失 → 上述全部路径同时恶化 → 战争有真实代价，不是「打完补兵继续打」。

---

## 三、完整数值表

### 3.1 `config/game_constants.csv` 常量清单

> **本次修订变更（净减 5 个，净增 1 个）**：
> - 删除 `pop_morale_bonus`：防守加成效应已移除（防御是城墙+驻军决定的静态存量）。
> - 删除 `pop_saturation_ratio`：下沉至 CSV 每建筑 `laborSaturation` 字段。
> - 删除 `pop_prod_floor`：重命名为 `pop_labor_floor`，适用范围扩展至全部速率类建筑。
> - 删除 `pop_build_floor`：建造速度零人口下限为 1.0（即无人口加速，不是"保底加速"），无需独立常量。
> - 删除 `pop_build_speedup`：下沉至 CSV 每建筑 `laborBonusMax` 字段。
> - 新增 `pop_labor_floor = 0.75`：全局速率下限，替代原 `pop_prod_floor`，覆盖所有速率类建筑。

| key | value | type | 说明 |
|-----|-------|------|------|
| `pop_growth_per_prosperity` | 15 | number | 每点繁荣度每小时增长的人口数 |
| `pop_per_capita_crop` | 2.0 | number | 每人每小时消耗粮食量（决定软上限大小） |
| `pop_labor_floor` | 0.75 | number | 人口为 0 时速率类建筑的最低倍率（适用：资源田/学院/兵营/马厩/兵工厂/铁匠铺；防死亡螺旋） |
| `pop_death_recovery_ratio` | 0.30 | number | 战斗阵亡中转化为伤兵的比例（30% 变伤兵，70% 永久阵亡） |
| `pop_heal_time` | 3600 | number | 伤兵治愈基础时长（秒；满员时 2400s；未来医院建筑可通过 modifier 缩短） |
| `pop_heal_bonus` | 0.50 | number | 满员时伤兵治愈速度加成（0.50 → 耗时变 2400s；0 人时 3600s） |

> **调参优先级**：`pop_growth_per_prosperity`（控制恢复速度）和 `pop_per_capita_crop`（控制软上限大小）是最敏感的两个参数，上线后应重点监控并按玩家行为数据调整。

---

### 3.2 `config/buildings.csv` 新增/更新列

原有 `prosperityPerLevel` 列保留。本次新增三列：

- `laborAmplified`（int 0/1）：是否吃人口参与劳动力增幅
- `laborSaturation`（int）：Lv1 时的饱和阈值基数（资源田随 `prodGrowth^(L-1)` 指数增长；其他建筑随等级线性增长：`S(L) = laborSaturation × L`）
- `laborBonusMax`（float）：满员时的最大增幅（速率类=产率/研究/练兵/锻造的最大加成；建造类=时间最大缩减比例）

| id | code | name | prosperityPerLevel | laborAmplified | laborSaturation | laborBonusMax |
|----|------|------|-------------------|----------------|-----------------|---------------|
| 1 | main | 城镇中心 | 5 | 1 | 400 | 0.20 |
| 2 | warehouse | 仓库 | 5 | 0 | 0 | 0 |
| 3 | granary | 粮仓 | 5 | 0 | 0 | 0 |
| 4 | barracks | 兵营 | 5 | 1 | 300 | 0.25 |
| 5 | stable | 马厩 | 5 | 1 | 300 | 0.25 |
| 6 | workshop | 兵工厂 | 5 | 1 | 300 | 0.25 |
| 7 | academy | 学院 | 5 | 1 | 300 | 0.25 |
| 8 | smithy | 铁匠铺 | 5 | 1 | 250 | 0.25 |
| 9 | wall | 城墙 | 5 | 0 | 0 | 0 |
| 10 | rallypoint | 集结点 | 5 | 0 | 0 | 0 |
| 11 | woodcutter | 伐木场 | 5 | 1 | 500 | 0.25 |
| 12 | claypit | 采泥场 | 5 | 1 | 500 | 0.25 |
| 13 | ironmine | 铁矿 | 5 | 1 | 500 | 0.25 |
| 14 | cropland | 农田 | 5 | 1 | 500 | 0.25 |

**分类说明**：
- **吃人口（laborAmplified=1）**：资源田（产出速率）、学院（研究速率）、兵营/马厩/兵工厂（练兵速率）、铁匠铺（锻造速率）、城镇中心（建造速度）。
- **不吃人口（laborAmplified=0）**：仓库（存储容量=存量）、粮仓（存储容量=存量）、城墙（防御值/耐久=静态物理）、集结点（集结容量=存量）。

**饱和阈值速查**（资源田指数/其余线性，选 Lv1/Lv5/Lv10/Lv15）：

| 建筑 | Lv1 | Lv5 | Lv10 | Lv15 |
|------|-----|-----|------|------|
| 资源田（指数，×1.3^(L-1)） | 500 | 1159 | 3588 | 11113 |
| 学院（线性，×L） | 300 | 1500 | 3000 | 4500 |
| 兵营/马厩/兵工厂 | 300 | 1500 | 3000 | 4500 |
| 铁匠铺 | 250 | 1250 | 2500 | 3750 |
| 城镇中心 | 400 | 2000 | 4000 | 6000 |

> 新手村（pop ~500，建筑 Lv1）：资源田基本饱和（需500人）；学院/兵营 Lv1 勉强饱和（需300人）。
> 中期城（pop ~10000，建筑 Lv8）：所有建筑全部饱和（最高需 S_资源田Lv8 ≈ 2188，pop 远超）。

---

### 3.3 `config/units.csv` `popCost` 列（不变）

沿用设计初稿的 popCost 建议值，此表无变化：

| id | code | name | form | popCost | 设计注释 |
|----|------|------|------|---------|---------|
| 1 | legionnaire | 军团兵 | melee | 1 | 步兵最低 |
| 2 | praetorian | 禁卫兵 | melee | 1 | |
| 3 | imperian | 帝国兵 | melee | 1 | |
| 4 | equlegati | 侦察骑兵 | melee | 2 | 骑兵类 |
| 5 | equimperatoris | 近卫骑兵 | melee | 3 | 重骑兵 |
| 6 | equcaesaris | 凯撒骑兵 | melee | 4 | 顶级重骑 |
| 7 | ram | 攻城锤(罗) | melee | 4 | 攻城高 |
| 8 | catapult | 投石机(罗) | ranged | 5 | 攻城最高 |
| 9 | senator | 元老 | melee | 10 | 首领很高 |
| 10 | settler | 拓荒者(罗) | melee | 5 | 较高，永久消耗 |
| 11 | phalanx | 方阵兵 | melee | 1 | |
| 12 | swordsman | 剑士 | melee | 1 | |
| 13 | pathfinder | 探路者 | melee | 1 | 步行侦察，低耗 |
| 14 | theutates | 雷神骑兵 | melee | 2 | |
| 15 | druidrider | 德鲁伊骑兵 | melee | 3 | |
| 16 | haeduan | 海杜安骑兵 | melee | 4 | |
| 17 | gaulram | 攻城锤(高) | melee | 4 | |
| 18 | gcaultrebuchet | 投石机(高) | ranged | 5 | |
| 19 | gaulchief | 首领(高卢) | melee | 10 | |
| 20 | gaulsettler | 拓荒者(高) | melee | 5 | 永久消耗 |
| 21 | clubswinger | 棍棒兵 | melee | 1 | |
| 22 | spearman | 长矛兵 | melee | 1 | |
| 23 | axeman | 斧兵 | melee | 1 | |
| 24 | teuscout | 侦察兵(条) | melee | 1 | |
| 25 | paladin | 圣骑士 | melee | 2 | |
| 26 | teutonknight | 条顿骑士 | melee | 4 | |
| 27 | teuram | 攻城锤(条) | melee | 4 | |
| 28 | teucatapult | 投石机(条) | ranged | 5 | |
| 29 | teuchief | 首领(条顿) | melee | 10 | |
| 30 | teusettler | 拓荒者(条) | melee | 5 | 永久消耗 |

---

## 四、数值演算示例

> 所有数值基于真实配置：prodBase=1000, prodGrowth=1.3, timeBase=1.5s, timeGrowth=1.6, start_resources=750。
> 建筑维护使用 `sumPop(L) = L×(L+1)/2` 公式（现有系统，不变）。

### 4.1 各阶段人口发展对比

下表按三部族开局配置（各 7 栋建筑，总等级均为 7）和典型发展路径展开：

#### 部族开局（新村）差异

| 部族 | 开局建筑 | 粮田等级 | 粮食产量/hr | 软上限人口 |
|------|----------|---------|------------|----------|
| 罗马 | main+rallypoint+**barracks**+4资源田(各Lv1) | Lv1 × 1 | 1000 | **≈495** |
| 高卢 | main+rallypoint+3资源田Lv1+**cropland Lv2** | Lv2 × 1 | 1300 | **≈645** |
| 条顿 | main+rallypoint+**woodcutter Lv2**+3资源田Lv1 | Lv1 × 1 | 1000 | **≈495** |

- 罗马开局有兵营可立刻训练，但人口上限最低。
- 高卢粮田起步 Lv2，软上限比罗马/条顿高约 30%，可训练更多初始军队。
- 条顿高级伐木场加快早期建筑升级，间接加速繁荣度提升，但粮食情况与罗马相同。

全三部族开局繁荣度 = 7 级 × 5 = **35**，增长速率 = 35 × 15 = **525 人/hr**。新村满员（从 1 增长到软上限）耗时约 1 小时。

#### 四阶段对比表

| 阶段 | 代表配置 | 繁荣度 | 增长/hr | 粮食产量/hr | 建筑维护/hr | 军队维护/hr | 软上限/均衡人口 |
|------|----------|--------|---------|------------|------------|------------|--------------|
| **新村**（开局） | 7栋Lv1，1块Lv1粮田 | 35 | 525 | 750–1000 | 7 | 0 | **≈495** |
| **早期** | 10栋均Lv3，2块Lv3粮田 | 150 | 2,250 | 3,380 | 60 | 300 | **≈1,510** |
| **中期** | 10栋均Lv8，4块Lv8粮田 | 400 | 6,000 | 25,100 | 360 | 3,000 | **≈10,870** |
| **晚期** | 12栋均Lv15，4块Lv15粮田 | 900 | 13,500 | 157,496 | 1,440 | 10,000 | **≈73,028** |

> 中期配置推导：
> - 4块Lv8粮田（满员饱和）：4 × 1000 × 1.3^7 = 4 × 6275 = 25,100/hr
> - 10栋Lv8维护：10 × sumPop(8) = 10 × 36 = 360/hr
> - 2000步兵维护（avg 1.5/hr）= 3000/hr
> - 软上限 = (25100-360-3000)/2.0 = **10,870人**

> 晚期配置推导：
> - 4块Lv15粮田：4 × 1000 × 1.3^14 = 4 × 39374 = 157,496/hr
> - 12栋Lv15维护：12 × sumPop(15) = 12 × 120 = 1,440/hr
> - 5000混合军队（avg 2/hr）= 10,000/hr
> - 软上限 = (157496-1440-10000)/2.0 = **73,028人**

---

### 4.2 粮食–人口反馈闭环收敛演算

**这是本系统的核心稳定性证明。** 以下仅对**农田劳动力饱和**论证收敛，其他建筑（学院/练兵/锻造/建造）的劳动力增幅不参与粮食–人口反馈回路，无需另行论证。

**反馈回路（仅涉及农田）**：人口 P 增加 → 农田劳动比提升 → 粮食产率上升 → 软上限 L 提高 → P 有更多增长空间 → P 继续增加...

如果这个循环没有制动，将导致 P 和 L 互相追逐、无限螺旋（失控）。以下证明在给定参数下系统**必然收敛**到唯一稳定均衡点。

#### 农田劳动力饱和公式

等级 L 农田的饱和阈值（`pop_labor_floor=fl=0.75`，`laborBonusMax=0.25`，`laborSaturation_cropland=500`）：

```
S(L) = 500 × prodGrowth^(L−1) = 500 × 1.3^(L−1)

实际粮食产率(L) = F_0(L) × [fl + (1−fl) × min(1, P/S(L))]
               = F_0(L) × [0.75 + 0.25 × min(1, P/S(L))]
```

其中 `F_0(L) = 1000 × 1.3^(L-1)`。

#### 固定点方程

均衡时 P = L（人口正好等于软上限），以 Lv1 单农田为例（`F_0=1000, S=500`，建筑维护 `U_b=7`，无军队维护），整理得：

$$P^* = \frac{F_0 \cdot fl + F_0 \cdot (1-fl) \cdot \frac{P^*}{S} - U_b}{c}$$

其中：$fl$ = 0.75，$S$ = 500，$U_b$ = 7，$c$ = `pop_per_capita_crop` = 2.0。

整理（仅当 P* < S，即未完全饱和时）：

$$P^* = \frac{F_0 \cdot fl - U_b}{c - F_0 \cdot \frac{1-fl}{S}} = \frac{1000 \times 0.75 - 7}{2.0 - 1000 \times \frac{0.25}{500}} = \frac{743}{1.5} \approx 495$$

**收敛条件**：分母 > 0，即 $S > F_0 \cdot \frac{(1-fl)}{c} = \frac{1000 \times 0.25}{2.0} = 125$

当前设计：$S_{Lv1} = 500 \gg 125$，**收敛安全余量 4 倍**。

**稳定性系数**（环路增益）：

$$\text{增益} = \frac{d(\text{软上限})}{dP} = \frac{F_0 \cdot (1-fl)}{c \cdot S} = \frac{1000 \times 0.25}{2.0 \times 500} = 0.25$$

增益 = 0.25 < 1：每轮"人口增长放大效应"只有 25%，每轮迭代误差收缩 75%，**几何级数收敛**。

#### 新村收敛迭代演示（罗马开局，1块Lv1农田，无军队）

从 P=0 出发，追踪软上限迭代轨迹（每一行代表 P 从上一行的 L 开始稳定后的快照）：

| 迭代 | 当前 P | 劳动比 P/S | 粮食产率/hr | 净粮（扣维护）/hr | 软上限 L | 差值 L-P |
|------|--------|-----------|------------|----------------|---------|---------|
| 0 | 0 | 0/500=0 | 750 | 743 | **371.5** | +371.5 |
| 1 | 371.5 | 0.743 | 935.8 | 928.8 | **464.4** | +92.9 |
| 2 | 464.4 | 0.929 | 982.3 | 975.3 | **487.7** | +23.3 |
| 3 | 487.7 | 0.976 | 994.0 | 987.0 | **493.5** | +5.8 |
| 4 | 493.5 | 0.987 | 996.8 | 989.8 | **494.9** | +1.4 |
| 5 | 494.9 | 0.990 | 997.5 | 990.5 | **≈495.3** | ≈0 |

**结论**：5 次迭代后误差 < 0.5，均衡点 P* ≈ 495，与解析解（≈495）一致。每轮差值缩减约 **75%**，符合理论增益 0.25。

#### 为何均衡是稳定的

当 P* ≈ 495（此时 P < S=500，接近饱和临界点）：

- 若外力把 P 推高到 500：此时 P > 软上限（=495），粮仓开始消耗 → P 被向下拉回 495。
- 若外力把 P 压低到 400：此时 P < 软上限（≈471，因劳动比降低），P 向上增长 → 回到 495。

**唯一稳定吸引子**，双侧自愈。

#### 完全饱和后（P ≥ S）的更简单情形

当城市发展到 P >> S（所有农田劳动力饱和），反馈回路断开，软上限退化为简单线性公式：

$$L = \frac{F_{max} - U_b - U_m}{c}$$

此时 L 不依赖 P，是纯粹的「粮食产量决定人口上限」，没有任何反馈复杂性。中/晚期城市均处于此状态（中期 P*=10870 >> S_单农Lv8=2188）。

---

### 4.3 战斗损失后的伤兵与恢复示例

**场景**：中期村庄，均衡人口 10,870，拥有 2,000 名罗马步兵（popCost=1，upkeep=1）。

**步骤一：出征前状态**
- 训练 2000 步兵时已消耗 2000 pop → `currentPop = 10870`（这是平衡后的状态）
- 软上限 = (25100-360-2000)/2.0 = 11370（扣军队维护后）

**步骤二：战斗结束，损失 500 步兵**

```
伤兵数  = floor(500 × 1 × 0.30) = 150 人  → 进入伤兵队列
永久阵亡 = 500 × 1 − 150       = 350 人  → 彻底消失
```

战后立刻：`currentPop = 10870`（未变，因 pop 在训练时已扣）；`woundedPool = [{count:150}]`

**步骤三：状态调整**
- 剩余兵力：1500，军队维护 = 1500/hr
- 新软上限 = (25100-360-1500)/2.0 = **11620**（比战前更高，因军队维护减少了）
- 当前 P=10870 < 新 L=11620 → 人口开始增长（速率 6000/hr），约 2 分半钟就能增加 250 人

**步骤四：伤兵治愈**
- λ = 10870/11620 ≈ 0.94（近满员）
- 实际治疗时长 = 3600 / (1 + 0.50×0.94) ≈ **2450s（约 41 分钟）**
- `currentPop += 150` → pop 回补 150 人

**净人口损失盘点**（从这场战斗看）：
- 350 人永久阵亡 → **永久损失 350 pop**
- 150 人伤兵经 ~41 分钟治愈后回归 → 有效回收 150 pop
- **实际净损失：350 pop（即 70% × 500）**

对比主动解散（不打仗直接解散）：500 pop 100% 归还，0 永久损失。战争代价得以量化。

---

### 4.4 劳动力增幅的数值示例

以中期村庄为基准（P=10870，soft_limit=10870，建筑均 Lv8）。中期所有建筑满员饱和（r=1.0），速率类 rate_mult=1.0，建造 time_mult_pop=0.80。

#### ① 资源产率

`S(8) = 500 × 1.3^7 ≈ 2188`，P=10870 >> S → r=1.0。

| 状态 | 劳动比 r | 每块Lv8农田产率 | 4块总粮食/hr |
|------|---------|--------------|------------|
| **满员**（r=1.0） | 1.0 | 6275×1.000 = **6,275/hr** | 25,100/hr |
| **半员**（r=0.5） | 0.5 | 6275×0.875 = **5,491/hr** | 21,964/hr |
| **零人口**（r=0） | 0 | 6275×0.750 = **4,706/hr** | 18,825/hr |

差值（满员 vs 零人口）：粮食 +33%，同步影响软上限。

#### ② 研究速率（学院 Lv8）

`S(8) = 300 × 8 = 2400`，P=10870 >> S → r=1.0。

| 状态 | r | rate_mult | 某科目研究耗时（基础 60 分钟） |
|------|---|-----------|------------------------------|
| 满员 | 1.0 | **1.000** | **60 分钟** |
| 半员 | 0.5 | **0.875** | 60/0.875 = **68.6 分钟**（+14%） |
| 零人口 | 0 | **0.750** | 60/0.750 = **80 分钟**（+33%） |

#### ③ 练兵速率（兵营 Lv8）

`S(8) = 300 × 8 = 2400`，P=10870 >> S → r=1.0。

| 状态 | rate_mult | 训练 100 步兵耗时（基础 1s/兵） |
|------|-----------|-------------------------------|
| 满员 | **1.000** | 100 秒 |
| 半员 | **0.875** | 100/0.875 = **114 秒**（+14%） |
| 零人口 | **0.750** | 100/0.750 = **133 秒**（+33%） |

#### ④ 锻造速率（铁匠铺 Lv8）

`S(8) = 250 × 8 = 2000`，P=10870 >> S → r=1.0。

| 状态 | rate_mult | 某兵种锻造升级耗时（基础 10 分钟） |
|------|-----------|----------------------------------|
| 满员 | **1.000** | **10 分钟** |
| 半员 | **0.875** | 10/0.875 = **11.4 分钟** |
| 零人口 | **0.750** | 10/0.750 = **13.3 分钟** |

#### ⑤ 建造加速（城镇中心 Lv8）

`S(8) = 400 × 8 = 3200`，P=10870 >> S → r=1.0（time_mult_pop=0.80）。

| 状态 | time_mult_pop | 某建筑升级（基础时间 180s） | 主基地Lv8叠加（-35%后再叠乘） |
|------|-------------|---------------------------|--------------------------|
| 满员（r=1.0） | **0.80** | 180×0.80 = **144s** | 180×0.65×0.80 = **94s** |
| 半员（r=0.5） | **0.90** | 180×0.90 = **162s** | 180×0.65×0.90 = **105s** |
| 零人口（r=0） | **1.00** | **180s** | 180×0.65 = **117s** |

> 主基地 Lv8 的等级加速 = min(60%, 7 × 5%) = 35%，time_factor = 0.65。

#### 新手阶段（pop=300，建筑均Lv2）参考

| 建筑 | S(2) | r=300/S(2) | rate_mult | 体感 |
|------|------|-----------|-----------|------|
| 农田 Lv2 | 500×1.3=650 | 0.46 | **0.865** | 产率 87% |
| 学院 Lv2 | 300×2=600 | 0.50 | **0.875** | 研究慢 14% |
| 兵营 Lv2 | 300×2=600 | 0.50 | **0.875** | 练兵慢 14% |
| 城镇中心 Lv2（建造时间） | 400×2=800 | 0.375 | time=1-0.20×0.375=**0.925** | 建造慢 8% |

新手期人口不足时，各速率有约 10–13% 削减，感知温和，不至于严重卡手（最坏情形 -25%）。

---

## 五、影响面与模块边界

### 5.1 新模块 `modules/population.ts`（Owner）

**唯一状态归属**：人口相关数据只由此模块拥有，其他模块不得直接读写其 store 集合。

```typescript
// population.ts 拥有的状态结构
interface PopulationState {
  villageId: string;
  currentPop: number;             // 当前人口（不含伤兵）
  woundedPool: WoundEntry[];      // 伤兵队列
  lastTick: number;               // 上次惰性结算的时间戳(ms)
}

interface WoundEntry {
  count: number;                  // 这批伤兵的人口数量
  healAt: number;                 // 治愈时间戳(ms)，由 Scheduler 触发
}
```

**初始化**：新村创建时，`currentPop = 软上限(初始粮食条件)`（新村开局满员）。

### 5.2 对现有模块的改动

| 模块 | 改动内容 | 铁律检查 |
|------|---------|---------|
| `infra/config.ts` | `GameConstants` 新增 `popLaborFloor`；删除 `popMoraleBonus / popSaturationRatio / popProdFloor / popBuildFloor / popBuildSpeedup`；`BuildingDef` 新增 `laborAmplified / laborSaturation / laborBonusMax / prosperityPerLevel`；`UnitDef` 新增 `popCost` | 仅加/删字段，不破坏现有结构 |
| `config/buildings.csv` | 新增列 `prosperityPerLevel / laborAmplified / laborSaturation / laborBonusMax`（见 3.2 表） | 加列不改逻辑 |
| `config/units.csv` | 新增列 `popCost`（见 3.3 表） | 加列不改逻辑 |
| `config/game_constants.csv` | 删除 5 个常量，新增 `pop_labor_floor=0.75`（见 3.1 表） | 加/删行不改逻辑 |
| `modules/building.ts` | 建筑完工/升级时：① 计算 `prosperity = Σ(level×prosperityPerLevel)` 并发出 `building.ProsperityUpdated` Event；② 向 population 发 `population.NotifyBuildingChanged` Command，通知人口模块重算该建筑劳动力快照 | 派生值在 owner 内部计算，对外发 Event，符合铁律 #4 |
| `modules/economy.ts` | 增加资源产率修正器：订阅 `population.LaborSnapshotUpdated` Event，按各资源田的 `laborMult` 快照注入资源产率派生管线 | 只读快照，符合铁律 #4 |
| `modules/military.ts` | 训练开始时：① 向 population 发 `population.ConsumeForTraining` Command 扣人口；② 按 `population.GetLaborMult(barracks/stable/workshop)` 读取劳动倍率，调整 trainSec；解散时发 `population.RestoreFromDissolution` Command | 跨模块走 Command，符合铁律 #2 |
| `modules/academy.ts`（待建） | 发起研究时读取 `population.GetLaborMult(academy)` 快照，调整研究耗时 | 只读快照，符合铁律 #2/#4 |
| `modules/smithy`（待建或合并进 military） | 锻造升级时读取 `population.GetLaborMult(smithy)` 快照，调整升级耗时 | 只读快照，符合铁律 #2/#4 |
| `modules/combat.ts` | 战斗结束时：发 `population.AddWounded` Command 给攻守双方各自的村庄（携带 `woundedCount`）。**不再**读取人口防御快照（防御效应已删除） | 跨模块走 Command，符合铁律 #2 |
| `packages/shared/wire.ts` | 新增 `PopulationSnapshotPush` 推送类型（currentPop, softLimit, woundedCount, lambdaRatio, laborMults） | 加新推送类型，不改信封格式 |

### 5.3 对外 Command / Event 接口规范

#### Commands（population.ts 注册，其他模块调用）

| Command | 调用方 | payload | 成功返回 | 失败返回 |
|---------|--------|---------|---------|---------|
| `population.GetSnapshot` | gateway | `{villageId}` | `{currentPop, softLimit, woundedCount, lambdaRatio, laborMults}` | — |
| `population.GetLaborMult` | military, economy, academy, building | `{villageId, buildingKind}` | `{mult: number}` | — |
| `population.ConsumeForTraining` | military | `{villageId, amount}` | `{ok:true}` | `{ok:false, reason:'insufficient_population'}` |
| `population.RestoreFromDissolution` | military | `{villageId, amount}` | `{ok:true}` | — |
| `population.AddWounded` | combat | `{villageId, woundedCount}` | `{ok:true}` | — |
| `population.NotifyBuildingChanged` | building | `{villageId, kind, level}` | `{ok:true}` | — |

#### Events（population.ts 发出）

| Event | 时机 | payload | 谁关心 |
|-------|------|---------|-------|
| `population.Changed` | 每次结算后 pop/softLimit 变化 | `{villageId, currentPop, softLimit, lambdaRatio}` | gateway（推给客户端） |
| `population.LaborSnapshotUpdated` | 建筑等级变化或人口变化导致劳动力快照更新 | `{villageId, laborMults: Record<string,number>}` | economy（更新资源产率）、military（更新练兵速率）、academy（更新研究速率）、building（更新建造加速） |

#### Events（population.ts 订阅）

| Event | 来源 | 用途 |
|-------|------|------|
| `building.ProsperityUpdated` | building.ts | 更新繁荣度 → 重算增长速率 |

#### 时间驱动

- **惰性结算**：人口增长与粮食消耗均采用 economy 同款惰性结算模式——`update(now)` 调用时按时间差 `Δt = now - lastTick` 补算，而非每 tick 推进。
- **伤兵治愈**：向 Scheduler 注册 `healAt` 时刻的一次性任务，到时发 `population.HealWounded` 内部命令（禁止模块内用 setTimeout，符合铁律 #3）。
- **软上限变化检测**：每次结算时，若软上限低于当前人口，向 Scheduler 注册「粮仓耗尽后开始减员」逻辑（粮仓容量 / 日消耗速率 = 粮仓耗完时间，届时触发减员循环）。

### 5.4 四条铁律自查

| 铁律 | 遵守情况 |
|------|---------|
| **状态归属唯一** | `currentPop` / `woundedPool` 只在 `population.ts` 的 store 集合里；其他模块通过 Command 读写 |
| **跨模块只传 Command/Event** | military → `population.ConsumeForTraining`；combat → `population.AddWounded`；economy/military/academy/building → `population.GetLaborMult` 或订阅 `population.LaborSnapshotUpdated` Event；没有跨模块直接 import |
| **时间统一走 Scheduler** | 惰性结算（读时补算）+ 伤兵治愈走 Scheduler 注册，不用 setInterval/setTimeout |
| **派生属性对外只给快照** | `prosperity`、`laborMults`、`softLimit`、`lambdaRatio` 均在内部计算，对外 `GetSnapshot` / `GetLaborMult` 只返回结果值 |

---

## 六、平衡风险与上线监控指标

### 主要风险点

| 风险 | 描述 | 缓解措施 |
|------|------|---------|
| **死亡螺旋** | pop低→产率低→净粮减少→软上限低→pop更难恢复→更低 | 设 `pop_labor_floor=0.75`（产率不低于75%），确保有足量粮食驱动繁荣度增长；新村满员开局也提供初始缓冲 |
| **练兵速率差距过大** | 满员 vs 零人口练兵速度差 33%，大型战争后补兵明显变慢 | 上限 25% 是刻意设计（给战争"代价感"）；若玩家反馈太慢，调低 `laborBonusMax`（barracks/stable/workshop） |
| **学院劳动加速套利** | 玩家专门保留人口不练兵，最大化研究速率 | 无套利空间：高人口意味着更高粮食消耗，自然制约；研究加速是相对的速率增幅，不影响研究树解锁顺序 |
| **士兵无限伤兵刷** | 反复训练-战死-治愈-训练，等效无资源消耗刷军队 | 伤兵只回pop不回士兵；治愈后仍需重新花资源训练，路径不可利用 |
| **拓荒者pop成本阻塞扩张** | 3名拓荒者=15 pop，新村人口<495可能不够 | 新村开局即满员（495+），训练3拓荒者消耗15 pop后剩480+；不构成阻塞 |
| **人口增长过快（晚期）** | 晚期繁荣度900，增长13500/hr，战后1-2小时满员 | 这是意图行为：晚期城市应快速恢复以维持博弈活跃度；若体感不爽，调低 `pop_growth_per_prosperity` |

### 上线后监控指标

| 指标 | 采样方法 | 告警阈值 | 对应调参 |
|------|---------|---------|---------|
| **平均战后pop恢复时间**（达80%软上限） | 战报+定时快照 | >4hr（晚期）/ >2hr（中期）→ 太慢 | 上调 `pop_growth_per_prosperity` |
| **练兵速率感知**（满员vs战损后速度差） | 玩家调研 + 训练时长分布 | >30%反馈"练兵太慢" → 劳动加成过强 | 下调 `laborBonusMax`（barracks/stable/workshop） |
| **拓荒者训练失败率**（insufficient_population） | 训练拒绝日志 | >20% → 新手被pop卡住 | 调低 settler popCost 或提高新村初始pop |
| **粮食软上限贴近率**（P/L 均值）| 每5分钟快照 | <60% → pop没成为有效约束 | 上调 `pop_per_capita_crop` |
| **伤兵治愈利用率**（伤兵治愈量/总阵亡量） | 医疗日志 | <10% → 玩家忽视此机制 | UI优化，或提高 `pop_death_recovery_ratio` |

---

## 附录：关键参数速查

| 常量/字段 | 建议值 | 一句话含义 |
|---------|-------|---------|
| `pop_growth_per_prosperity` | **15** | 繁荣度35的新村每小时增长525人 |
| `pop_per_capita_crop` | **2.0** | 人均耗粮；决定软上限规模（越大→软上限越小→约束越紧） |
| `pop_labor_floor` | **0.75** | 零人口时所有速率类建筑的产率下限；防经济/研究/练兵/锻造/建造完全停摆 |
| `pop_death_recovery_ratio` | **0.30** | 30%阵亡转伤兵；70%永久消失；战争有真实人口代价 |
| `pop_heal_time` | **3600** | 满员时40分钟愈合；零人口时1小时；为「医院」建筑预留扩展空间 |
| `laborSaturation`（资源田） | **500** | Lv1饱和阈值500人；收敛安全余量4倍；Lv8≈2188人 |
| `laborSaturation`（学院/兵营/马厩/兵工厂） | **300** | 每级需300人；Lv8=2400人；中期城自然满员饱和 |
| `laborSaturation`（铁匠铺） | **250** | 每级需250人；略低于学院 |
| `laborSaturation`（城镇中心） | **400** | 每级需400人；Lv8=3200人；中期城自然满员饱和 |
| `laborBonusMax`（资源田/学院/兵营/马厩/兵工厂/铁匠铺） | **0.25** | 满员时速率+25%（×1.0）；零人口时×0.75 |
| `laborBonusMax`（城镇中心） | **0.20** | 满员时建造节省20%；与主基地等级加速叠乘 |
| `prosperityPerLevel` (buildings.csv) | **5** | 建筑每级5点繁荣度；全部均等，军事≡经济 |
